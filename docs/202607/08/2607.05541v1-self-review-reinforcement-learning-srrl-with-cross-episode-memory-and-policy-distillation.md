---
title: Self-Review Reinforcement Learning (SRRL) with Cross-Episode Memory and Policy Distillation
title_zh: 自审强化学习（SRRL）结合跨情节记忆与策略蒸馏
authors: "Muhammad Zain Amin, Kibele Sebnem Yildirim"
date: 2026-07-06
pdf: "https://arxiv.org/pdf/2607.05541v1"
tags: ["query:policy-dist"]
score: 9.0
evidence: 带跨episode记忆和策略蒸馏的自反思强化学习
tldr: 强化学习训练大语言模型时，环境反馈稀疏且延迟，模型难以定位推理中导致成败的具体动作。本文提出自审强化学习框架，在RL回合中嵌入显式自审步骤：首轮响应失败时，模型生成自审以识别错误，并据此改进第二次尝试。该方法通过策略梯度优化自审，并利用选择性蒸馏将改进持久化到基础策略，确保跨回合保留。此外，跨回合记忆存储成功自审，便于未来相似任务重用。在GSM8K上使用Qwen3-4B和OLMo-7B评估，SRRL相比标准RLVR基线在最终奖励和学习效率上均更优。该工作有效地将反馈转化为行为改进，提升了RL训练效果。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2607.05541v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 860, \"height\": 1758, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2607.05541v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1768, \"height\": 732, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2607.05541v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 871, \"height\": 357, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.05541v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 487, \"height\": 190, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.05541v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 865, \"height\": 392, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2607.05541v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 854, \"height\": 578, \"label\": \"Table\"}]"
motivation: 现有RL训练LLM时反馈稀疏延迟，模型难以从失败中学习有效改进。
method: 在RL回合中嵌入自审步骤，首轮失败时生成自审，通过策略梯度优化并利用选择性蒸馏内化改进，跨回合记忆重用成功案例。
result: 在GSM8K上，SRRL在Qwen3-4B和OLMo-7B上均优于RLVR基线，最终奖励更高且学习更高效。
conclusion: SRRL有效将反馈转化为持久行为改进，提升了RL训练效率和模型性能。
---

## 摘要
强化学习通常用于利用环境反馈训练大型语言模型。在实际应用中，环境通常提供稀疏或延迟的反馈，这使得模型难以确定推理中哪些行为导致了成功或失败。因此，从这些信号中有效学习是困难的，因为模型必须确定每个失败应如何为后续迭代中有意义的行为修正提供信息。我们提出了一种训练框架——自审强化学习，它将显式的自审步骤嵌入到每个强化学习情节中。当首次响应失败时，模型会生成自审以识别错误原因，从而条件化改进的第二次尝试。与推理时反思方法（如Reflexion）不同，该框架通过策略梯度优化自审，并通过选择性蒸馏将改进内化到基础策略中，确保其在未来情节中持续存在。跨情节记忆在训练过程中保留成功的自审，以便在遇到类似任务时重用。我们在GSM8K基准上使用GRPO优化器，对Qwen 3-4B和OLMo-3-7B两种语言模型，将SRRL与标准RLVR基线进行了评估。SRRL在最终奖励性能上始终优于RLVR，并通过成功将反馈转化为行为改进实现了更高的学习效率。

## Abstract
Reinforcement Learning is commonly used to train large language models using environmental feedback. In applied settings, the environment usually provides sparse or delayed feedback. This makes it difficult for the model to pinpoint which actions in its reasoning led to success or failure. So, learning effectively from these signals is hard because the model must determine how each failure should inform meaningful behavioral corrections in subsequent iterations. We introduce a training framework, Self-Review Reinforcement Learning, that embeds an explicit self-review step into each RL episode. When a first-pass response fails, the model generates a self-review to identify what went wrong, which conditions an improved second attempt. Unlike inference-time reflection approaches, such as Reflexion, the framework optimizes self-review with policy gradients and internalizes improvements into the base policy via selective distillation, ensuring they persist across future episodes. A cross-episode memory keeps successful self-reviews for reuse when encountering similar tasks in future episodes during training. We evaluate SRRL against a standard RLVR baseline using the GRPO optimizer across two language models, Qwen 3-4B and OLMo-3- 7B, on GSM8K benchmark. SRRL consistently outperforms the RLVR in final reward performance and achieves greater learning efficiency by successfully transforming feedback into behavioral improvement.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义

- **研究动机**：强化学习（RL）训练大型语言模型时，环境反馈通常是稀疏、延迟的标量奖励，模型难以定位推理过程中导致成功或失败的具体步骤。标准 RLVR（Reinforcement Learning with Verifiable Rewards）缺乏结构化反馈，迫使模型通过重复探索来隐式推断错误原因，导致样本效率低下、行为修正不稳定。
- **整体含义**：论文提出一种新型训练框架 **Self-Review Reinforcement Learning (SRRL)**，通过在 RL 情节内嵌入显式自审步骤、跨情节记忆和策略蒸馏，将环境反馈转化为结构化的行为修正信号，并持久内化到基础策略中，从而提升训练效率和最终性能。

## 2. 论文提出的方法论

- **核心思想**：在标准 RLVR 的基础上，为每个训练情节增加一个条件触发的自审环节：
  - 首轮响应 y1 若失败（奖励 r1 < 阈值 τ），模型生成自审文本 Δ，识别错误并给出修正策略。
  - 以 Δ 为条件生成第二轮改进响应 y2。
  - 若 y2 成功，将 Δ 存入跨情节记忆，供后续类似任务重用；同时通过选择性蒸馏损失训练基础策略直接从输入 x 生成 y2 水平的响应（无需自审）。
- **关键技术细节**：
  - **自审生成**：Δ ∼ πθ(x, y1, r1, f1, m)，其中 f1 为文本反馈，m 为记忆。
  - **第二轮生成**：y2 ∼ πθ(x, Δ)。
  - **跨情节记忆**：仅存储成功第二轮的 Δ，作为未来检索上下文。
  - **选择性蒸馏**：Loss_distill = -E[ I{r2>τ} log πθ(y2 | x) ]，只对成功的第二轮响应进行监督学习。
  - **RL 优化器**：采用 GRPO（Group Relative Policy Optimization），对首轮、自审和第二轮都计算策略梯度损失。
  - **总损失**：L_SRRL = L_policy + L_distill + β·L_KL（β=0.001）。
  - **部署特性**：自审仅在训练时触发，推理时模型只需 πθ(·|x)，零额外延迟。

## 3. 实验设计

- **数据集与场景**：GSM8K（小学数学文字题），每道题有单一数字答案，环境提供二值奖励（正确=1，错误=0），是典型的稀疏奖励推理任务。
- **基准**：标准 RLVR 基线（无自审、无记忆、无蒸馏），同样使用 GRPO 优化器。
- **对比方法**：只对比了 RLVR，未与其他反思方法（如 Reflexion、Self-Refine）做直接比较，但指出 SRRL 的训练式自审避免了推理时开销。
- **评估指标**：验证集奖励轨迹（学习效率）和最终测试集奖励。
- **验证集/测试集**：验证集 473 样本，测试集 1319 样本。

## 4. 资源与算力

- **GPU 型号**：NVIDIA H100。
- **GPU 数量与训练时长**：文中未明确说明使用了多少块 GPU，也未给出具体训练时长（仅提到训练了 219 步，每步梯度累积 32 个 episode）。
- **其他资源**：使用 8-bit AdamW 优化器，梯度累积有效 batch size 为 32。

## 5. 实验数量与充分性

- **实验规模**：共训练了两个模型（Qwen3-4B 和 OLMo-3-7B），每个模型分别运行 RLVR 和 SRRL，验证曲线和最终测试结果均报告。未进行额外的消融实验（如去除跨情节记忆、去除蒸馏等）。
- **公平性**：作者强调总训练计算量相等（RLVR 每 prompt 6 个 rollout，SRRL 每 prompt 分两轮各 3 个 rollout，总计 6 个）。超参数基本一致（学习率、KL 系数、裁剪率等）。
- **充分性评估**：实验覆盖了两个不同架构（Qwen 和 OLMo）和两种参数规模（4B 和 7B），但仅在一个任务（GSM8K）上验证，缺乏多任务推广性证明。未进行超参数敏感性分析，也未分析记忆检索的具体实现效果。总体而言，实验设计较扎实但不够全面。

## 6. 论文的主要结论与发现

- SRRL 在最终奖励性能上始终优于标准 RLVR：Qwen3-4B 上提升约 1.6%（0.762→0.778），OLMo-3-7B 上提升约 2.8%（0.714→0.742）。
- SRRL 的学习效率更高：验证奖励曲线更快上升并保持领先。
- 自审对数学先验弱的模型（OLMo-3-7B）提升更大，说明结构化修正信号在模型基础能力不足时更有帮助。
- 训练过程中验证与测试奖励一致，未出现过拟合。

## 7. 优点

- **训练式自审**：将自审步骤集成到 RL 训练中，避免了推理时反思（如 Reflexion）带来的额外计算和延迟。
- **选择性内化**：通过蒸馏将成功的修正策略直接写入基础策略参数，使改进持久化且跨情节可迁移。
- **跨情节记忆**：重用已验证的有效自审，避免重复探索相同错误。
- **条件触发机制**：只对失败的首轮响应应用自审，防止成功样本被污染或引发奖励破解。
- **实验设计公平**：保持总 rollout 数相等，控制变量。

## 8. 不足与局限

- **任务覆盖不足**：仅在 GSM8K 一个数学推理任务上评测，未在代码生成、科学问答、对话等更广泛场景中验证。
- **缺乏消融实验**：未单独分析跨情节记忆、选择性蒸馏、自审触发阈值等组件各自的贡献，难以量化每个模块的效果。
- **训练步数有限**：仅 219 步训练，模型可能未完全收敛，尤其是 OLMo-3-7B 奖励曲线仍呈线性上升趋势。
- **记忆检索实现未详述**：如何从记忆库中检索相关 Δ 未给出具体算法或消融，可能影响可复现性。
- **超参数敏感性未知**：奖励阈值 τ（设为 1）和蒸馏系数 λ（设为 1.0）是固定值，未讨论其影响。
- **基线对比范围窄**：仅与标准 RLVR 对比，未与 Reflexion、Self-Refine、Self-Distillation 等最新方法进行比较。
- **计算资源信息不完整**：未明确 GPU 数量、训练时间，不利于实际部署评估。

（完）
