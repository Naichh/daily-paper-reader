---
title: "Rethinking Reward Supervision: Rubric-Conditioned Self-Distillation"
title_zh: 重新思考奖励监督：基于评分准则的条件自蒸馏
authors: "Siyi Gu, Jialin Chen, Sophia Zhou, Arman Cohan, Rex Ying"
date: 2026-06-17
pdf: "https://arxiv.org/pdf/2606.19327v1"
tags: ["query:policy-dist"]
score: 9.0
evidence: 基于评分标准的在线策略自蒸馏用于后训练
tldr: 推理语言模型后训练常用监督蒸馏和强化学习，但蒸馏依赖昂贵且可能不准确的思维链，强化学习标量奖励反馈不足。本文提出Rubric-Conditioned Self-Distillation，利用规则评分作为结构化细粒度反馈进行在线自蒸馏，教师依据rubric提供token级指导。在科学推理基准上平均超越GRPO 1.0分、OPSD 0.9分。该方法将rubric标准转化为推理过程的token级信用分配，提升学习效率与泛化能力。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.19327v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 696, \"height\": 514, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.19327v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1440, \"height\": 539, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.19327v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1293, \"height\": 440, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.19327v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1432, \"height\": 337, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.19327v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1291, \"height\": 240, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.19327v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1325, \"height\": 313, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.19327v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1261, \"height\": 204, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.19327v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 704, \"height\": 279, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.19327v1/table-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1230, \"height\": 225, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.19327v1/table-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 1306, \"height\": 240, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.19327v1/table-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1242, \"height\": 314, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.19327v1/table-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1218, \"height\": 355, \"label\": \"Table\"}]"
motivation: 现有监督蒸馏依赖昂贵且不准确的思维链，强化学习标量奖励反馈粗粒度，无法细粒度指导推理过程。
method: 提出Rubric-Conditioned Self-Distillation，先学习生成任务特定rubric，再以此进行token级在线自蒸馏。
result: 在科学推理基准上平均超越GRPO 1.0分、OPSD 0.9分。
conclusion: 将rubric标准转化为token级指导，有效实现细粒度信用分配，提升推理过程学习效果。
---

## 摘要
推理语言模型的后训练通常由监督式蒸馏和基于可验证奖励的强化学习驱动。蒸馏通常依赖于代价高昂的思维链标注，这些标注本身可能包含噪声、不完整或部分错误；即使最终解决方案正确，不完善的推理过程也会干扰学习。另一方面，使用已验证奖励的强化学习通常将评估反馈压缩为标量信号，从而模糊了应答中需要改进的方面。我们提出\textbf{基于评分准则的条件自蒸馏}，这是一个将评分准则作为结构化、细粒度反馈用于在策略自蒸馏的框架。我们的方法将教师模型以准则级评分准则为条件，并利用它为学生的自主采样轨迹提供词元级指导。这种设计避免了将单一参考推理过程作为唯一的监督目标。相反，评分准则明确了强应答应满足的条件，从而比标量奖励优化更能在推理过程中实现细粒度的信用分配。我们通过两阶段流程实例化该框架：首先学习生成任务特定的评分准则，然后训练评分准则引导的推理器。我们在多种科学推理基准上进行了评估，结果表明，基于评分准则的条件自蒸馏能有效将评分准则级别的标准转化为推理过程中的词元级别指导，平均超过GRPO 1.0分，超过OPSD 0.9分。

## Abstract
Post-training of reasoning language models is commonly driven by supervised distillation and reinforcement learning with verifiable rewards. Distillation often relies on chain-of-thought annotations that are expensive to obtain and may themselves be noisy, incomplete, or partially incorrect; even when the final solution is correct, an imperfect rationale can interfere with learning. Reinforcement learning with verified rewards, on the other hand, typically compresses evaluative feedback into a scalar signal, obscuring which aspects of a response should be improved. We propose \textbf{Rubric-Conditioned Self-Distillation}, a framework that incorporates rubrics as structured, fine-grained feedback for on-policy self-distillation. Our method conditions the teacher model on criterion-level rubrics and uses it to provide token-level guidance on the student's own sampled trajectories. This design avoids treating a single reference rationale as the sole supervision target. Instead, rubrics specify what a strong response should satisfy, enabling more fine-grained credit assignment over the reasoning process than scalar reward optimization. We instantiate this framework with a two-stage pipeline that first learns to generate task-specific rubrics and then trains a rubric-guided reasoner. We evaluate on a diverse suite of science reasoning benchmarks and results show that rubric-conditioned self-distillation effectively converts rubric-level criteria into token-level guidance over the reasoning process, surpassing GRPO by 1.0 points and OPSD by 0.9 points on average.

---

## 论文详细总结（自动生成）

# 论文详细中文总结

## 1. 论文的核心问题与整体含义（研究动机和背景）

- **问题**：当前推理语言模型的后训练主要依赖两种范式：
  - **监督蒸馏**：使用昂贵的思维链标注作为监督，但标注可能含噪声、不完整或部分错误，即使最终答案正确，不完善的推理过程也会干扰学习。
  - **强化学习（如 GRPO）**：依赖可验证的奖励信号（如答案正确性），但将评估反馈压缩为单一标量，无法区分推理步骤中的局部错误，导致信用分配瓶颈。
- **核心挑战**：如何提供既细粒度（token 级别）又结构化（多维度）的监督信号，避免单一参考轨迹的偏见，同时不引入额外奖励模型或昂贵标注。
- **整体含义**：本文提出利用**评分准则（Rubrics）**——即一组结构化的、细粒度的评价标准——作为特权教师信息，在在线自蒸馏框架中实现 token 级的、准则感知的监督，从而提升推理模型的学习效率和泛化能力。

## 2. 论文提出的方法论：核心思想、关键技术细节

### 2.1 核心思想
- **Rubric-Conditioned Self-Distillation (RCSD)**：将评分准则作为特权教师侧的监督，用于在线自蒸馏。
- 关键区别：
  - 传统 RL：只给整个轨迹一个标量奖励。
  - 传统 OPSD：教师以单一参考答案为条件，提供 token 级监督，但路径特定，缺乏准则级信用分配。
  - RCSD：教师以**评分准则**（结构化的一组标准）为条件，提供 token 级指导，保留多维度评估信息，同时是 on-policy 的（学生自身采样轨迹）。

### 2.2 关键技术细节
- **两阶段流程**：
  - **Stage I：学习评分准则生成器**。训练一个模型，给定问题，生成实例特定的评分准则（一组标准，每个标准含标题、描述、重要性权重）。教师可访问问题+参考答案，学生只基于问题生成。采用在线蒸馏：学生生成自己的准则轨迹，教师提供 token 级分布匹配（前向 KL 散度）。
  - **Stage II：训练评分准则引导的推理器**。学生仅基于问题生成推理轨迹，教师以问题+评分准则（由 Stage I 生成器提供）为条件，提供 token 级监督。损失函数为学生轨迹上的前向 KL 散度。
- **损失函数**：使用**前向 KL 散度**（Forward KL），因为它鼓励学生覆盖教师的条件分布，保留多样化的有效推理路径；相比反向 KL 更不模式寻求，相比 JSD 更有效传递信号。
- **目标公式**：  
  Stage I: \( L_{rubric} = E_{\hat{r} \sim p_S} [\sum_t D_{KL}(p_T(\cdot|x, y^*, \hat{r}_{<t}) \| p_S(\cdot|x, \hat{r}_{<t}))] \)  
  Stage II: \( L_{reason} = E_{\hat{y} \sim p_S} [\sum_t D_{KL}(p_T(\cdot|x, r, \hat{y}_{<t}) \| p_S(\cdot|x, \hat{y}_{<t}))] \)

## 3. 实验设计：数据集、基准、对比方法

- **数据集**：
  - 训练集：来自 RaR-Science（Gunjal 2025）、RubricHub（Li 2026）、natural_reasoning（Yuan 2025），共约 10k 样本用于 rubric 生成、30k 样本用于推理训练。
  - 评估集：
    - 可验证类：GPQA-Diamond、SciBench、PIQA、ResearchQA。
    - 非可验证类（基于 rubric 的 LLM-as-a-Judge 评估）：RaR-Science、RubricHub。
    - 领域泛化：MedMCQA、PubMedQA。
- **基准（对照方法）**：
  - 基础模型：Qwen3-8B（骨干）。
  - SFT：监督微调（蒸馏 CoT 轨迹）。
  - GRPO：标准组相对策略优化，使用 LLM-as-a-Judge 输出标量奖励。
  - GRPO-Rubrics：使用 rubrics 奖励（聚合为标量）。
  - OPSD：在线自蒸馏，教师以参考答案为条件。
  - RCSD（本文方法）。
- **评估设置**：对可验证任务使用精确匹配；对开放任务使用 GPT-4.1-mini 作为 LLM-as-a-Judge，按照论文原有设置。

## 4. 资源与算力

- **文中提及**：
  - 使用 LoRA（r=64, α=128）进行训练。
  - AdamW 优化器，学习率 5×10⁻⁶，batch size 32，FlashAttention 3。
  - Stage I 训练 1000 步（最大序列长度 2048），Stage II 训练 100 步（最大完成长度 4096）。
  - GRPO 训练 500 步。
- **未明确说明**：GPU 型号、数量、总训练时长。因此无法详细总结算力消耗。

## 5. 实验数量与充分性

- **实验组数**：
  - 主结果（Table 1）：在 6 个基准上与 4 个基线比较。
  - 领域泛化（Table 2）：2 个医疗基准。
  - 消融实验：
    - 损失类型（Table 3）：Forward KL vs. Reverse KL vs. JSD，并附训练动态图（Figure 3）。
    - 评分准则来源（Table 4）：GT Rubrics vs. Generated Rubrics。
    - 模型规模（Figure 4）：1.7B、4B、8B 三个尺度，3 个基准。
    - Stage I 必要性（Table 5）：直接使用更大模型生成 rubrics vs. 学习生成器。
    - 评分准则质量退化（Table 6）：Generic、Noisy、Random、Reduced 变体。
  - 训练步数分析（Figure 5）：GPQA-D 和 SciBench 上不同 checkpoint 性能。
  - 案例研究（Appendix C.1）：理想气体问题的具体输出对比。
  - 失败分析（Appendix C.2）：两个示例的 rubric 质量对比。
- **充分性**：实验设计全面，覆盖主要对比、多尺度、多种退化、动态分析，且包含定性分析。实验较为充分、客观。消融实验公平地控制了变量（如相同训练步数、相同 backbone）。

## 6. 论文的主要结论与发现

- RCSD 在**平均性能**上最优（70.6），超过 GRPO（69.2）1.4 分，超过 OPSD（69.7）0.9 分。
- 在非可验证的 rubric 推理任务（ResearchQA、RubricHub）上增益明显（分别提升 8.2 和 4.9 分）。
- 前向 KL 散度作为蒸馏损失最有效，能保持学生熵稳定增长，避免模式坍缩。
- 学习的 rubric 生成器（Stage I）产生的 rubric 性能接近真实 rubric，且比直接提示大模型更高效。
- 即使使用噪声或随机 rubric，RCSD 仍能提升性能，表明教师能够在一定程度上补偿低质量 rubric。
- 模型规模影响：RCSD 在不同规模（1.7B、4B、8B）上均一致提升。
- 案例研究显示 RCSD 生成更简洁、一致、稳定的推理轨迹，避免了 OPSD 的冗余和基础模型的数值波动。

## 7. 优点：方法或实验设计上的亮点

- **方法创新**：首次将评分准则（rubrics）作为特权教师信息，用于在线自蒸馏的 token 级监督，巧妙结合结构化准则和密集信号。
- **两阶段设计**：自动生成 rubric，避免人工标注，且 Stage I 的学习不依赖外部奖励模型。
- **损失函数选择**：精细对比 Forward KL、Reverse KL、JSD，并给出理论和实验选择理由。
- **实验详尽**：
  - 覆盖可验证和非可验证任务。
  - 领域泛化测试（医疗）。
  - 多种消融（rubric 质量、规模、损失类型、训练步数）。
  - 定性分析（案例与失败分析）增强说服力。
- **公平性**：与基线在相同设置（相同训练步数等）下比较。
- **开源**：提供代码仓库。

## 8. 不足与局限

- **算力信息缺失**：未报告 GPU 型号、数量、训练时间，难以复现或评估资源需求。
- **训练规模较小**：Stage II 仅 100 步，更长训练是否带来进一步提升未清楚；基线 GRPO 训练 500 步，步数不等可能影响公平性（但作者声明遵循先前工作设置）。
- **rubric 质量依赖**：虽然鲁棒，但 Stage I 生成器仍可能产生冗余或未对准的 rubric（如附录失败分析所示），尽管最终影响不大，但潜在性能受损。
- **领域限制**：主要基于科学推理，其他领域（如数学、代码、通用 QA）未充分验证；医疗泛化虽保持，但未超越 GRPO 或 OPSD。
- **只测试了 Qwen 系列**：未在更多不同架构（如 Llama、DeepSeek）上验证，可能受限于模型特性。
- **评估非可验证任务**：依赖 LLM-as-a-Judge（GPT-4.1-mini），可能引入评估偏差，尽管使用已有设置。
- **理论分析较少**：除 Proposition 1 外，缺乏更深入的理论解释为什么 rubric 条件蒸馏优于 reference 条件蒸馏。

（完）
