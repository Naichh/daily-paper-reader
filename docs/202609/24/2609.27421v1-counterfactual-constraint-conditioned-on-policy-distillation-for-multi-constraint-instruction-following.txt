Title: Counterfactual Constraint-Conditioned On-Policy Distillation for Multi-Constraint Instruction Following

URL Source: https://arxiv.org/pdf/2609.27421v1

Published Time: Thu, 24 Sep 2026 00:38:59 GMT

Number of Pages: 29

Markdown Content:
## Counterfactual Constraint-Conditioned On-Policy Distillation for Multi-Constraint Instruction Following 

Yanzhao Zheng, Yuanqiang Yu, Tianze Xu, Chao Ma, Zhentao Zhang Jihuai Zhu, Baohua Dong *, Hangcheng Zhu, Ruohui Huang 

Alibaba Group, Hangzhou, China 

{zhengyanzhao.zyz,yuyuanqiang.yyq,xutianze.xtz}@alibaba-inc.com {mc524716,zhangzhentao.zzt,zhujihuai.zjh}@alibaba-inc.com {baohua.dbh,linran.lr09,wentong}@alibaba-inc.com 

Abstract 

Multi-constraint instruction following requires a model to respond to a query under many si-multaneously active constraints. Even strong instruction-tuned models still routinely violate some of them. Existing approaches either aug-ment supervision with sequence- or token-level RL rewards from external verifiers or learned graders, or use on-policy distillation (OPD) against a single full-context teacher whose probability mass becomes diluted as more con-straints become simultaneously active. We propose CC-OPD (Counterfactual Constraint-Conditioned On-Policy Distillation), which in-verts the standard supervision-generation di-rection in distillation. Rather than enriching the teacher with information beyond what the student sees, CC-OPD ablates each constraint from the teacher’s conditioning in turn, and con-structs the per-constraint signal from the result-ing per-token probability differentials. The re-sulting per-token leave-one-out log-likelihood shifts are summed, clipped, and added to the vanilla OPD reward as a token-level shaping term. All shaping terms are obtained from the frozen teacher, without an external veri-fier during distillation, and the reward equals vanilla OPD wherever the aggregate shift is zero. Across two Qwen model pairs and seven benchmarks, CC-OPD achieves the highest average among all evaluated student-training methods. A 1.5B student trained with CC-OPD surpasses its own 7B RL-trained teacher on the MulDimIF benchmark. 

1 Introduction 

Modern LLM assistants are routinely deployed un-der user instructions that simultaneously specify multiple requirements such as a target format, a length cap, a style register, content to include or avoid, a safety policy, or a citation convention.  

> *Corresponding author. Code: github.com/zhengyanzhao1997/cc-opd

Each request bundles these into a single natural-language prompt, and the system is judged by whether the response satisfies all of them at once. Recent benchmarks make clear that this multi-constraint regime is far from solved, and when a prompt carries several simultaneously active con-straints, even strong instruction-tuned models rou-tinely violate some of them (Zhou et al., 2023; Pyatkin et al., 2025; Ye et al., 2025). The harder and more numerous the constraints, the larger this gap becomes, and closing it is now a central post-training challenge for instruction-following LLMs. Two lines of post-training research are com-monly applied to multi-constraint instruction fol-lowing, but each leaves a structural piece of the con-straint signal unused. Rubric- or checklist-based reinforcement learning methods augment training with externally graded constraint signals. They elicit constraints from the prompt, grade each cri-terion with a verifier or LLM judge, and aggregate criterion-wise scores into a scalar or token-level RL reward (Fang et al., 2026; Xu et al., 2026; Gun-jal et al., 2025; Viswanathan et al., 2025; Zhou et al., 2025). On-policy distillation (OPD) (Agar-wal et al., 2024; Gu et al., 2024; Ko et al., 2024; Li et al., 2026), in contrast, is a general post-training framework that distills the student directly from a frozen teacher’s token distribution. When applied to multi-constraint instruction following, OPD con-ditions the teacher on the full instruction, which collapses the constraint set into a single joint con-ditioning context. As more constraints become si-multaneously active, the full-context teacher must spread its probability mass across many require-ments, and an individual constraint’s contribution to the per-token distribution becomes increasingly difficult to disentangle. We call this effect con-straint dilution . Neither line extracts per-constraint information directly from the teacher. We propose CC-OPD (Counterfactual Constraint-Conditioned On-Policy Distillation), arXiv:2609.27421v1 [cs.LG] 23 Sep 2026 0 5000 10000 15000 20000       

> Gradient Steps
> 0.5 0.6 0.7 0.8
> MulDimIF Overall Accuracy
> (a) Overall Accuracy
> 05000 10000 15000 20000
> Gradient Steps
> 0.4 0.6 0.8
> MulDimIF Level-4 (hardest)
> (b) Level-4 (hardest)
> Qwen2.5-1.5B-CC-OPD Qwen2.5-1.5B-OPD Qwen2.5-7B-GRPO (teacher)

Figure 1: Student surpasses its own teacher. MulDimIF training trajectories on the Qwen2.5-1.5B pair (Qwen Team et al., 2024) vs. gradient steps. CC-OPD (cyan) crosses the 7B teacher’s accuracy ceiling (red dashed) on both (left) the overall score and (right) the hardest Level-4 split, while vanilla sampled-token OPD (black) plateaus below the teacher. 

which derives constraint-indexed shaping signals through counterfactual teacher scoring while keep-ing the OPD KL backbone unchanged. The student remains conditioned on the full instruction at both training and deployment. For each student rollout, the frozen teacher scores the same response under the full instruction and under each leave-one-out context C \ { ci}. The resulting log-likelihood differences yield per-token, per-constraint signals 

δi(t), which we sum, clip, and add to the vanilla sampled-token OPD reward. Because each contrast is computed before aggregation, the resulting 

|C| × T signal retains the identity of individual constraints; wherever the aggregate shift is zero, the reward equals vanilla sampled-token OPD. A 1.5B student trained with CC-OPD sur-passes its own 7B RL-trained teacher on multi-dimensional constraint following by +2 .8 pp on the MulDimIF overall score and +6 .3 pp on its hardest difficulty split (Figure 1). This is a rare outcome for OPD (Li et al., 2026), where students typically re-cover only part of the teacher–student performance gap rather than crossing it. Across both Qwen set-tings and seven instruction-following benchmarks, CC-OPD attains the highest average among all eval-uated student-training methods, including recent OPD variants. Our contributions are threefold. First, we in-troduce CC-OPD, a leave-one-out teacher-scoring method that produces a token-level signal for each constraint on student rollouts (§3). Second, we characterize the resulting signal algebraically: the LOO sum assigns cardinality-dependent weights to Möbius interaction terms, and a zero aggregate shift leaves the vanilla sampled-token OPD reward unchanged (§3; Appendix B). Third, we evaluate CC-OPD against SFT, verifier-based RL, vanilla OPD, Teacher-TopK RKL/SRKL, EOPD, ExOPD, and prompt-augmentation variants, together with counterfactual-design and training-efficiency anal-yses (§4.2). 

2 Preliminaries 

2.1 Multi-Constraint Instruction Following 

We consider multi-constraint instruction following ,in which a model must produce a response y to a user query q that simultaneously satisfies a set of natural-language constraints C = {c1, . . . , c N }.Each ci is a self-contained sentence describing one requirement (length, formatting, taboos, tone, ci-tation format, and similar constraints). The query and constraint set are concatenated into a single natural-language instruction inst (q, C ) in which the constraints appear as inline clauses interleaved with the query. Throughout we use C for the full constraint set, |C| for its cardinality, and ci for the 

i-th constraint; in realistic scenarios |C| is rarely small, with typical samples carrying several simul-taneously active constraints. 

2.2 On-Policy Distillation with Sampled-Token PPO 

On-policy distillation (OPD) distills a frozen teacher PT into a student πθ by minimizing a token-level divergence on sequences sampled from the student itself. Given an input (q, C ) and a student rollout y = ( y1, . . . , y L) ∼ πθ(· | q, C ), the canon-ical OPD objective is 

LOPD = Ey∼πθ

> L

X

> t=1

KL  πθ(· | ht) PT (· | ht) ,

(1) teacher 

> scoring
> contexts
> 3) Per-constraint
> counterfactual
> attribution
> 1) Student rollout
> from full instruction

⌣ Student 

> Input to student:
> (query / prompt)

+       

> full (constraint set)
> ⋮
> Sample response y
> y= ( y₁,y₂,⋯,yL)
> Student rollout (same across all teacher contexts)

⋯ ⋯        

> 2) Frozen teacher PTforced-scores the exact same yunder N +1constraint contexts
> full
> ⋮
> leave out
> ⋮
> leave out
> ⋮
> leave out
> ⋮

⋯            

> δi(t) =
> ℓ( C;t) −
> ℓ( C\ { ci}; t)
> constraints × tokens
> ⋮
> ⋯⋯
> + high
> 0
> − low
> 4) Sum over constraints δ(t) = ∑ iδi(t)
> 5) Clip δ̂ (t) = clip( δ(t), − c, + c)
> 6) Add to vanilla
> OPD reward (token-level reward)
> positive attribution
> (helps under full )
> near zero
> (no change)
> negative attribution
> (hurts under full )ℓ(·; t): teacher per-token log-prob (forced scoring)
> 7) PPO update
> (Student only)
> ⌣
> Student
> (updated)
> PPO update
> maximize
> using token-level
> reward
> (e.g., clipped PPO)
> ⌣
> Frozen
> Teacher
> (no updates)
> •

Figure 2: CC-OPD pipeline. On the same student rollout y, the frozen teacher PT is forced-scored under |C| + 1 

counterfactual prompts (steps 1–2), namely the full set C and each leave-one-out context C \ { ci}. The resulting per-constraint shifts δi(t) (step 3) are summed, symmetrically clipped, and additively composed with the vanilla sampled-token OPD reward (steps 4–6) to form the token-level reward rt used in a standard ratio-clipped PPO update (step 7). Only the prompt varies across teacher contexts, while y is shared. 

where ht = ( q, C, y <t ) is the shared decoding state at step t. Direct minimization of (1) requires mate-rializing the full per-token vocabulary distribution of both models, which is memory-bound at large vocabulary sizes. We therefore adopt the sampled-token PPO (Schulman et al., 2017) formulation of OPD, in which the per-token KL is replaced by its single-sample stochastic estimate at the realized student token, 

cKL( t) = log πθ(yt | ht) − log PT (yt | ht), (2) and the resulting scalar rOPD 

t = − cKL( t) is treated as a token-level reward in standard ratio-clipped PPO. Only one teacher and one student log-probability per token need to be computed. Re-cent work shows that under appropriately tuned training settings the sampled-token form attains end-task performance comparable to top-k OPD variants (Li et al., 2026). 

3 CC-OPD 

The sampled-token OPD reward of Eq. (2) treats the constraint set C as a single conditioning con-text, so every constraint contributes to PT jointly and the per-token signal cannot tell whether yt was supported by c1, by c7, or by no constraint at all. As |C| grows, this constraint dilution (§1) wors-ens, since the teacher must spread its probability mass across many simultaneously active require-ments and individual contributions become difficult to disentangle from the joint signal. CC-OPD re-stores per-constraint resolution while keeping the student conditioning unchanged. The student re-mains conditioned on the full C at both training and deployment, while the frozen teacher is queried 

|C| + 1 times on the same student rollout, once un-der the full instruction and once per constraint with that constraint removed from its natural-language context. The resulting log-likelihood shifts form a dense, per-token, per-constraint signal that is ag-gregated into a token-level shaping term and added to the vanilla OPD reward (Figure 2). 

3.1 Per-Constraint Counterfactual Teacher Scoring 

Let inst (q, C ) denote the natural-language instruc-tion associated with sample (q, C ). For each ci, we construct a counterfactual prompt by excising the natural-language span corresponding to ci from 

inst (q, C ). The same student rollout y is then forced-scored by the teacher under each of these |C| + 1 prompts; let 

ℓ(A; t) = log PT

 yt q, A, y <t 

 , A ⊆ C, 

(3) denote the resulting log-probability under any con-straint subset A. The per-constraint counterfactual signal for constraint ci at token t is the leave-one-out log-likelihood shift 

δi(t) = ℓ(C; t) − ℓ(C \ { ci}; t) . (4) A positive δi(t) means the teacher assigns more probability to the realized student token because ci

is in the prompt, identifying a constraint-supporting token. A value near zero indicates that ci is irrel-evant to that token, and a negative value indicates that ci suppresses yt, either because the token vio-lates ci or because removing ci shifts the teacher’s prior in an unrelated direction. Because the student rollout y is held fixed across all |C| + 1 teacher passes, the contrast in (4) isolates the marginal ef-fect of ci from the joint effect of the remaining constraints, without paying for extra rollouts. 

Why leave-one-out rather than single-constraint instantiation. A more naive design might local-ize each constraint’s contribution by querying the teacher with that constraint in isolation, comput-ing δi(t) = ℓ({ci}; t) − ℓ(∅; t). This is conceptu-ally equivalent to constructing N single-constraint teachers and reading off each one’s standalone ef-fect. Such a design discards all inter-constraint interactions, since in multi-constraint settings the way the teacher enforces ci depends on which other constraints are simultaneously active, and these dependencies cannot be recovered from a single-constraint forward pass. We evaluate this alterna-tive empirically in §4.5 (LOO vs. NAO variants). 

3.2 Counterfactual Reward Shaping 

The signal δi(t) is per-token and per-constraint, but PPO consumes a single scalar advantage per token. We aggregate over constraints by summation, 

δ(t) = 

> |C|

X

> i=1

δi(t), (5) and apply a symmetric hard clip with threshold c

to prevent any single outlier token (typically an end-of-sequence symbol, a rare lexical item, or a numerically pathological context) from dominating the policy gradient, 

ˆδ(t) = clip( δ(t), −c, +c) . (6) The CC-OPD token-level reward composes the clipped counterfactual signal additively with the vanilla OPD reward of (2), 

rt = − cKL( t)

| {z }

> vanilla OPD

+ λ ˆδ(t)

| {z } 

> CC-OPD shaping

, (7) and the resulting rt enters a standard ratio-clipped PPO update. Throughout the paper we use λ = 2 .0

and c = 5 .0, whose sensitivities are analyzed in Appendix E. Algorithm 1 in Appendix A gives the complete training step as drop-in pseudocode. 

Reduction to vanilla OPD at zero aggregate shift. 

When the aggregate counterfactual shift satisfies 

δ(t) = P 

> i

δi(t) = 0 , Eq. (6) gives ˆδ(t) = 0 , and Eq. (7) reduces exactly to vanilla sampled-token OPD at that token. When δ(t) is small but nonzero and lies within the clipping range, clipping leaves it unchanged, so CC-OPD differs from vanilla OPD by the correspondingly small additive term λδ (t).In all cases, Eq. (6) bounds the absolute shaping contribution by λc per token. 

Backbone-agnostic composition. Equation (7) writes the CC-OPD reward as the sum of an OPD KL term and the shaping term λˆδ(t). Only the first term depends on the form of the OPD KL estima-tor; the shaping term is constructed entirely from teacher-side forced-scoring log-likelihoods and is independent of the student’s KL form. The shaping term can therefore be added to OPD objectives that expose a per-token reward signal; our experiments use sampled-token reverse-KL. A Möbius decomposition of (4) and an exponential-tilt interpretation of (7) are given in Appendix B. Token-level reward maps are visual-ized in Appendix C, and alternative aggregations of δi(t) are ablated in Appendix D. 

4 Experiments 

4.1 Experimental Setup Model pairs. We evaluate CC-OPD on two student–teacher pairs spanning two model fami-lies and two student sizes. The Qwen2.5 pair uses Qwen2.5-1.5B-Instruct (Qwen Team et al., 2024) as the student and a Qwen2.5-7B teacher obtained by training Qwen2.5-7B-Instruct with GRPO-IR (instruction-level reward; see Baselines below) on the HIR-16K instruction-following corpus (Zhang et al., 2025a). The Qwen3 pair uses Qwen3-4B-Instruct-2507 (Yang et al., 2025) as the student and a same-size teacher obtained by training Qwen3-4B-Instruct with GRPO-IR on the same corpus. Each teacher is GRPO-IR-tuned to obtain a strong instruction-following reference before distillation, following the standard practice of task-adapting the teacher prior to OPD. This setup provides a strin-gent evaluation because the student is compared against a teacher that has already been adapted to the target instruction-following corpus. 

Baselines. We compare against several train-ing paradigms, each instantiated for both model families. SFT fine-tunes the student on teacher-generated responses. Two GRPO (Shao et al., 2024) variants instantiate rubric-RL: GRPO-IR ,matching RL-IR of Peng et al. (2025); Pyatkin et al. (2025), uses an instruction-level reward ( r=1 iff every constraint is satisfied, else 0), and GRPO-CR , matching RL-CR of Qi et al. (2025); Pyatkin et al. (2025), uses a constraint-level mean reward. Vanilla sampled-token OPD is the direct distilla-tion baseline. Three OPD-augmentation variants (OPD-aug-LOO/Rand/SC ) enlarge the OPD train-ing set with leave-one-out, random-subset, and single-constraint counterfactual prompts respec-tively. We additionally include two teacher-support controls: Teacher-TopK RKL on the teacher’s renormalized top-32 support, and Teacher-TopK SRKL , our top-k on-policy adaptation of Dis-tiLLM’s skewed reverse-KL loss ( α=0 .1) (Ko et al., 2024) on the same support. The remain-ing added baselines are entropy-aware EOPD 

(k=16 , τ =0 .8, α =1 .0) (Jin et al., 2026); Ex-OPD (Yang et al., 2026), for which we evaluate the original sweep settings λ ∈ { 1.25 , 1.5} and an additional λ = 2 .0 extrapolation setting; and 

RTT-GRPO (Xu et al., 2026), rubric-based RL using a learned Token-Level Relevance Discrimi-nator and joint response- and token-level advan-tages. For Qwen3, the GRPO-IR baseline and the teacher share weights but we list them sepa-rately in the results table. The GRPO baselines use verifier-derived rewards from rule-based code verifiers or an LLM judge during training. The CC-OPD update derives its reward from teacher log-probabilities without querying an external grader. 

Benchmarks. We evaluate on seven instruction-following benchmarks: IFEval (Zhou et al., 2023), IFBench (Pyatkin et al., 2025), MulDimIF (Ye et al., 2025), ComplexBench (Wen et al., 2024), InfoBench (Qin et al., 2024), FollowBench (Jiang et al., 2024), and CFBench (Zhang et al., 2025c). We report instruction-level accuracy for all bench-marks, defined as the percentage of model re-sponses that satisfy all constraints specified in the corresponding prompts. Full evaluation details and judge configuration are in Appendix A. 

Implementation. Vanilla OPD, the three OPD-augmentation variants, ExOPD, and CC-OPD use sampled-token PPO with ϵ = 0 .2. Teacher-TopK RKL and Teacher-TopK SRKL apply direct losses to distributions normalized over the teacher’s top-

32 support. EOPD combines the sampled-token reverse-KL OPD reward with an entropy-gated teacher-top-16 forward-KL loss. All methods are trained until validation performance saturates, and we report the corresponding saturated checkpoint. CC-OPD uses λ = 2 .0 and c = 5 .0 throughout; IFEval, IFBench, and MulDimIF were used to se-lect these values, while ComplexBench, InfoBench, FollowBench, and CFBench were not used in this selection and serve as transfer checks. Detailed training budgets, checkpoint selection, hyperpa-rameters, and SFT data construction are provided in Appendix A. 

4.2 Main Results 

Table 1 reports the seven-benchmark performance of CC-OPD against all baselines for both model pairs. CC-OPD achieves the best average score in both families: 51 .4% on Qwen2.5 ( +2 .5 pp over the strongest ExOPD setting) and 64 .3% on Qwen3 (+1 .5 pp over Teacher-TopK RKL). It leads on six of seven individual benchmarks for Qwen2.5 and is best or tied on five of seven for Qwen3. Be-yond average score, the CC-OPD-trained Qwen3 student surpasses its own RL-trained teacher (an-alyzed in §4.3). The Self-Distillation block at the bottom of Table 1 compares self-CC-OPD against DR-CFG (Cideron et al., 2025) under the same sampled-token OPD variant, replacing the exter-nal teacher with the student’s own initialization weights: self-CC-OPD leads on the Qwen3-4B pair by +0 .9 pp on the seven-benchmark average ( 61 .9

vs. 61 .0) and is close on the Qwen2.5-1.5B pair (37 .7 vs. 37 .5). Appendix A.3 provides implemen-tation details and per-benchmark self-distillation results. 

4.3 Student Surpasses Teacher 

Table 1 shows that CC-OPD-trained students cross their teacher’s accuracy ceiling under both model pairs on MulDimIF, a rare outcome for OPD. Model IFEval IFBench MulDimIF ComplexB InfoB FollowB CFB Avg. 

Qwen2.5 family – student: Qwen2.5-1.5B-Instruct, teacher: Qwen2.5-7B-Instruct trained with GRPO-IR Qwen2.5-1.5B-Instruct (untrained) 43.6 15.3 23.6 48.2 37.0 34.2 24.1 32.3 Teacher (Qwen2.5-7B + GRPO-IR) 80.5 32.1 70.1 66.7 52.1 51.9 51.2 57.8 SFT 66.7 21.6 65.4 50.8 41.9 38.2 29.6 44.9 GRPO-IR 54.3 18.6 36.0 51.1 39.9 35.0 27.8 37.5 GRPO-CR 56.6 19.2 49.0 52.3 44.1 39.6 31.4 41.7 RTT-GRPO 52.3 18.0 38.8 50.9 39.9 37.3 26.8 37.7 OPD 70.7 22.8 69.5 54.4 43.0 38.7 34.4 47.6 Teacher-TopK RKL ( k=32 ) 68.0 24.2 67.5 53.5 42.9 35.7 33.8 46.5 Teacher-TopK SRKL ( k=32 , α =0 .1) 66.9 23.8 68.9 53.2 44.3 37.2 32.4 46.7 EOPD ( k=16 , τ =0 .8) 69.7 24.2 70.1 54.5 43.3 37.7 33.6 47.6 ExOPD ( λ=1 .25 ) 73.9 24.8 70.3 55.2 43.0 39.4 35.2 48.8 ExOPD ( λ=1 .5) 71.2 22.8 71.0 54.3 43.5 37.6 35.7 48.0 ExOPD ( λ=2 .0) 75.0 22.4 71.3 53.8 43.7 39.1 36.5 48.9 OPD-aug-SC 72.5 25.6 69.3 54.1 42.3 39.8 33.9 48.2 OPD-aug-Rand 72.7 24.4 69.0 53.8 41.7 39.8 34.4 48.0 OPD-aug-LOO 72.1 23.7 69.1 54.3 42.5 39.0 33.8 47.8 

CC-OPD (ours) 76.0 28.2 72.9 54.8 46.4 43.8 37.8 51.4 

Qwen3 family – student: Qwen3-4B-Instruct-2507, teacher: Qwen3-4B-Instruct trained with GRPO-IR †

Qwen3-4B-Instruct (untrained) 83.8 29.9 56.9 70.6 55.3 57.8 61.2 59.4 Teacher (Qwen3-4B + GRPO-IR) † 83.4 32.5 74.3 71.2 57.7 58.1 62.7 62.8 SFT 82.1 29.7 71.7 69.7 56.3 57.5 61.9 61.3 GRPO-IR † 83.4 32.5 74.3 71.2 57.7 58.1 62.7 62.8 GRPO-CR 83.1 32.9 73.1 70.8 55.9 58.0 62.8 62.4 RTT-GRPO 84.7 31.3 74.6 71.3 55.7 57.8 61.8 62.5 OPD 84.8 29.8 74.0 69.3 55.8 57.4 62.0 61.9 Teacher-TopK RKL ( k=32 ) 84.7 29.9 75.2 70.9 58.4 57.8 62.7 62.8 Teacher-TopK SRKL ( k=32 , α =0 .1) 84.5 31.6 73.6 71.3 57.8 58.1 61.8 62.7 EOPD ( k=16 , τ =0 .8) 84.3 31.0 74.2 70.8 56.7 57.8 62.2 62.4 ExOPD ( λ=1 .25 ) 85.8 31.6 74.5 70.0 57.7 55.7 63.4 62.7 ExOPD ( λ=1 .5) 84.5 30.6 73.1 69.9 58.9 55.4 63.1 62.2 ExOPD ( λ=2 .0) 82.1 28.6 72.5 69.8 54.3 56.0 63.2 60.9 OPD-aug-SC 82.7 31.6 72.5 70.3 56.3 57.6 62.9 62.0 OPD-aug-Rand 82.8 29.9 74.8 70.3 56.4 56.9 62.4 61.9 OPD-aug-LOO 84.3 29.6 74.9 70.2 58.1 56.6 62.1 62.3 

CC-OPD (ours) 86.1 37.6 76.2 71.0 57.4 58.4 63.4 64.3 

Self-Distillation Comparison – teacher is replaced by the frozen student initialization weights DR-CFG (Qwen2.5-1.5B) 49.2 23.8 29.8 49.2 40.4 39.2 30.6 37.5 self-CC-OPD (Qwen2.5-1.5B) 56.9 19.6 30.0 50.3 39.6 38.9 28.3 37.7 

DR-CFG (Qwen3-4B) 84.7 32.8 61.3 70.0 55.9 59.9 62.6 61.0 self-CC-OPD (Qwen3-4B) 84.9 36.3 61.5 70.7 57.7 59.9 62.5 61.9 

Table 1: Main results across seven instruction-following benchmarks (% accuracy / pass rate; higher is better). Bold 

marks the best score in each column within its block , excluding reference rows (Instruct, Teacher, and the identical GRPO-IR † row in the Qwen3 family). The Avg. column averages the seven benchmark scores. † For the Qwen3 family, the teacher and the GRPO-IR baseline are the same model (Qwen3-4B-Instruct trained with GRPO-IR); we list them separately to keep the baseline grid uniform across families. 

On Qwen2.5, the 1.5B student reaches 72 .9 %on MulDimIF overall, +2 .8 pp above the 7B teacher’s 70 .1 %; the gap widens to +6 .3 pp on the constraint-saturated Level-4 split ( 59 .4 % vs. 

53 .1 %). On Qwen3, where the student and teacher share the same model size, the student exceeds the teacher on five of seven benchmarks (Com-plexBench and InfoBench are within 0.3 pp), show-ing that a teacher–student size gap is not required for teacher-surpassing performance. Figure 1 shows the validation trajectory of the Qwen2.5 student on MulDimIF crossing the teacher ceiling at roughly gradient step 1,500 ; vanilla sampled-token OPD on the same student asymptotes be-low it. Among the OPD variants trained on the Qwen2.5 pair, all three ExOPD settings also ex-0 20000 40000 0.50 0.55 0.60 0.65 0.70 0.75                        

> Qwen2.5-1.5B Accuracy
> 0.0 0.5 1.0 1.5 0.50 0.55 0.60 0.65 0.70 0.75 020000 40000 0.3 0.4 0.5 0.6 0.0 0.5 1.0 1.5 0.3 0.4 0.5 0.6 020000 40000
> Gradient Steps
> 0.55 0.60 0.65 0.70 0.75 0.80
> Qwen3-4B Accuracy
> 012
> ×10 20 FLOPs
> 0.55 0.60 0.65 0.70 0.75 0.80 020000 40000
> Gradient Steps
> 0.40 0.45 0.50 0.55 0.60 0.65 0.70 012
> ×10 20 FLOPs
> 0.40 0.45 0.50 0.55 0.60 0.65 0.70
> MulDimIF-Overall MulDimIF-Level-4
> CC-OPD OPD aug-single-constraint aug-leave-one-out aug-random-subset

Figure 3: MulDimIF training trajectories for five OPD-family methods. Top row: Qwen2.5-1.5B pair; bottom row: Qwen3-4B pair. Each row plots MulDimIF overall accuracy (columns 1–2) and Level-4 accuracy (columns 3–4) against gradient steps (columns 1, 3) and total FLOPs (columns 2, 4). The OPD baseline and three OPD-augmentation baselines are trained to saturation ( 4× more gradient steps than CC-OPD). 

ceed the 7B teacher on MulDimIF ( 70 .3–71 .3 % vs. 

70 .1 %), while CC-OPD attains the largest margin (72 .9 %). On the same-size Qwen3 pair, two OPD-augmentation variants also edge past the teacher on MulDimIF ( 74 .9 and 74 .8 vs. 74 .3), but CC-OPD attains the largest margin ( 76 .2). 

4.4 Gradient-Step Efficiency 

We anchor efficiency on the OPD baseline trajec-tory and ask, at each smoothed reference accuracy that OPD baseline reaches, how many gradient steps CC-OPD needs to match it (all accuracy val-ues are EMA-smoothed with weight 0.85 ). On the Qwen2.5-1.5B pair, CC-OPD matches OPD base-line’s smoothed MulDimIF peak in 15 .2× fewer gradient steps on the overall metric and 16 .6×

fewer on the hardest Level-4 split; OPD baseline’s trajectory never reaches CC-OPD’s eventual peak within its full 48 ,000 -step horizon. CC-OPD’s 

4.63 × per-step teacher overhead on Qwen2.5 is more than offset by this step reduction: at iso-accuracy CC-OPD uses ≈ 30% of OPD baseline’s total FLOPs on Qwen2.5 and ≈ 53% on Qwen3-4B (where the per-step overhead is smaller be-cause student and teacher share size). The three OPD-augmentation baselines fall in between (Fig-ure 3). Counterfactual subsampling (an additional efficiency lever) is ablated in Appendix F. 

4.5 Counterfactual Variant Ablation 

§3.1 defines CC-OPD’s per-constraint signal δi(t)

via the leave-one-out contrast ℓ(C; t) − ℓ(C \{ci}; t). We now compare this choice against three alternative counterfactual designs that vary the structure of the “with” and “without” constraint subsets used in the contrast. LA (Leave-All) col-lapses the contrast to ℓ(C) − ℓ(∅), producing one scalar shift per token rather than |C| per-constraint shifts. RSS (Random Subset) replaces C \ { ci}

with C \ Sj : for each job j, it first samples the removal size |Sj | uniformly from {1, . . . , |C| − 1}

and then samples Sj uniformly among subsets of that size, summing the resulting contrasts over 

j = 1 , . . . , |C|. NAO (No-context Anchor Only) replaces the full-context “with” anchor with sin-gleton contexts, ℓ({ci}) − ℓ(∅), summed over i =1, . . . , |C|. Full derivations and extended signal-level diagnostics for these four variants appear in Appendix B. Table 2 reports the seven-benchmark performance of all four variants on the Qwen2.5-1.5B pair, under identical training hyperparameters. The leave-one-out construction obtains the strongest average score (51.4 %, +0 .9 pp over LA and RSS, +2 .7 pp over NAO) and outper-forms LA on six of seven individual benchmarks (CFBench: 37 .8 vs. 38 .0). The largest cross-benchmark LOO–LA gaps occur on MulDimIF (+1 .9), ComplexBench ( +1 .3), and InfoBench Variant IFEval IFBench MulDimIF ComplexB InfoB FollowB CFB Avg. LOO (CC-OPD) 76.0 28.2 72.9 54.8 46.4 43.8 37.8 51.4                            

> LA 75.0 27.1 71.0 53.5 45.3 43.5 38.0 50.5 RSS 75.5 28.8 72.1 52.9 44.5 42.6 37.3 50.5 NAO 71.2 26.5 66.4 54.6 45.0 41.7 35.7 48.7
> Table 2: Counterfactual variant ablation on the Qwen2.5-1.5B pair (% accuracy / pass rate; higher is better). All four variants share identical training hyperparameters; they differ only in the structure of the “with” and “without” constraint subsets used to construct δi(t).Bold marks the best score in each column.

(+1 .1 pp), while the remaining gaps are IFEval 

+1 .0, IFBench +1 .1, FollowBench +0 .3, and CF-Bench −0.2 pp. 

Signal-level comparison of LOO and LA. For a fixed realized token yt, let ft(S) be its teacher log-probability under constraint subset S, and let 

mt(T ) denote the corresponding Möbius coeffi-cients (Rota, 1964). Then 

ft(S) = X 

> T⊆S

mt(T ),δLA (t) = X 

> ∅̸ =T⊆C

mt(T ),δLOO (t) = X 

> ∅̸ =T⊆C

|T | mt(T ).

(8) LA therefore weights every non-empty interaction uniformly, whereas LOO weights an order-k inter-action by k. Strict additivity across constraints is sufficient for the two signals to agree. Conversely, a nonzero LOO–LA residual certifies higher-order interactions, although a zero residual need not im-ply additivity because signed terms can cancel. We examine the raw signals on 15 ,450 HIR-16K examples with 5–10 constraints, using Qwen2.5-1.5B student rollouts scored by the Qwen2.5-7B teacher. The relative L1 residual is 0.918 , and the two signals disagree in sign on 31 .9% of tokens. The unsummed K ×T LOO sensitivity map is also structured: the dominant constraint accounts for 

42 .6% of absolute signal mass at an average to-ken, versus 15 .0% under uniform attribution, while pairs of constraints have mean top-20 affected-token Jaccard overlap 0.269 . The K×T LOO map retains these per-constraint sensitivities, while LA provides one full-versus-empty contrast per token. These statistics describe the unclipped teacher log-probability shifts produced by constraint ablation. Downstream benchmark results assess the learned policy after clipping and optimization. Appendix B gives the full derivations and additional statistics.                    

> Method L1 L2 L3 L4
> LA 86.9 74.8 65.3 57.1 LOO 89.2 75.3 67.6 59.4
> ∆(pp) +2 .3+0 .6+2 .2+2 .3
> Table 3: LOO and LA accuracy on the four MulDimIF difficulty tiers under the same training step, hyperpa-rameters, deterministic scoring protocol, and constraint vocabulary. Scores are rounded to one decimal; ∆is computed from unrounded values.

Controlled comparison across difficulty tiers. 

Table 3 compares LOO and LA within MulDimIF while holding the training step, hyperparameters, deterministic scoring protocol, and constraint vo-cabulary fixed. LOO improves accuracy on every tier: +2 .3, +0 .6, +2 .2, and +2 .3 pp on L1–L4. The gain is positive but not monotonic with diffi-culty. This pattern is consistent with Eq. (8): LA assigns unit weight to each non-empty Möbius co-efficient, whereas LOO weights a coefficient by its interaction order. The identity predicts a differ-ence when higher-order terms do not cancel, but not a steadily increasing performance gap, because the coefficients are signed and the aggregated re-ward is clipped before PPO. The positive L3/L4 gains show that the LOO advantage persists when more constraints are stacked, while the smaller L2 gap indicates that constraint count alone does not determine the benefit. 

Variant-level comparison. LA remains a use-ful, efficient approximation when teacher inference dominates cost (two teacher passes per sample vs. |C| + 1 for LOO and RSS). NAO ( δi(t) = 

ℓ({ci}; t) − ℓ(∅; t)) instantiates the naive “ N

single-constraint teachers” design rejected in §3.1. Its clear underperformance ( −2.7 pp Avg, −6.5 pp on MulDimIF, −14 .1 pp on the MulDimIF L4 tier in Appendix B.6) confirms that replacing the full-context anchor with a no-context anchor removes the full-context, per-constraint sensitivity structure used by LOO. These ablations support the full-context leave-one-out anchor as the default reward-shaping design. Sensitivity of the shaping strength 

λ and clip threshold c around our defaults is re-ported in Appendix E. 

5 Related Work 

On-policy distillation. On-policy distilla-tion (OPD) trains a student to match a teacher distribution on the student’s own rollouts. The framework was introduced by GKD (Agarwal et al., 2024) and refined in MiniLLM (Gu et al., 2024) and DistiLLM (Ko et al., 2024) through reverse-KL and skewed-KL variants, with recent analyses motivating the sampled-token formulation we adopt (Li et al., 2026). CC-OPD keeps this sampled-token KL backbone unchanged and adds a per-constraint counterfactual shaping term on the same rollout. 

Rubric-based RL for instruction following. Aclosely related line treats rubrics or checklists as the reward channel for instruction-following RL. ROPD (Fang et al., 2026), RaR (Gunjal et al., 2025), and RLCF (Viswanathan et al., 2025) aggre-gate criterion-wise verifier or LLM-judge scores into pass-rate, GRPO, or DPO rewards. RTT (Xu et al., 2026) trains an external token-level relevance discriminator, and RuscaRL (Zhou et al., 2025) in-jects rubrics into the actor’s rollout as exploration scaffolding before grading. All convert constraints into externally graded reward signals on the stu-dent side. CC-OPD instead inverts this design. Rather than enriching the supervision channel with external grading, CC-OPD ablates the teacher’s conditioning one constraint at a time and extracts per-constraint, per-token credit from the teacher’s leave-one-out forced-scoring shifts. 

Contrastive likelihood signals. Several meth-ods shape generation through log-probability con-trasts at different stages of the pipeline: CFG-LM (Sanchez et al., 2024), CAD (Shi et al., 2024), and Contrastive Decoding (Li et al., 2023) reweight logits at decoding time using prompt-vs-no-prompt or expert-vs-amateur contrasts (two forward passes, no parameter updates), and AlignDistil (Zhang et al., 2025b) performs training-time distillation against a DPO-derived log-ratio teacher. Diversity-Rewarded CFG Distillation (Cideron et al., 2025) is a training-time analog in text-to-music generation. It distills a single CFG-augmented teacher (instan-tiated from the frozen student initialization) into a single-pass student via a single full-prompt-vs-negative-prompt contrast. CC-OPD decomposes the contrast across each individual constraint via leave-one-out rather than collapsing C into one prompt-vs-no-prompt contrast. 

6 Conclusion 

We presented CC-OPD, a counterfactual constraint-conditioned on-policy distillation method that ex-tracts per-constraint signal from a frozen teacher’s leave-one-out forced scoring on the same student rollout. Across two Qwen settings and seven instruction-following benchmarks, CC-OPD at-tains the highest average among the evaluated student-training methods in both settings. It also yields the largest student-over-teacher MulDimIF margin among the tested OPD variants. More broadly, our results reframe constraint-conditioned supervision from an additive paradigm, in which the supervision channel is enriched with external rubrics or graders, to an ablative one, in which the same frozen teacher is queried across counterfac-tual conditioning subsets, exposing per-constraint supervision that no single full-context forward pass produces. 

Limitations 

CC-OPD is trained on HIR-16K and evaluated on two Qwen-family student–teacher pairs (Qwen2.5-1.5B/7B and Qwen3-4B/4B). Generalization to other instruction-following corpora, to non-Qwen model families, and to multi-turn or system-prompt-steerable settings remains untested. The method assumes a white-box teacher avail-able for forced scoring: per-token log-probability access under multiple counterfactual prompts is required, so teachers exposed only through gen-eration APIs are outside the scope of the current construction. Our experiments evaluate CC-OPD only with the sampled-token reverse-KL OPD objective. Its effectiveness with other OPD objectives has not been tested. The counterfactual prompt construction further assumes that each sample’s constraints can be cleanly excised from the natural-language instruc-tion via byte-aligned span deletion; non-inline con-straint formats such as structured fields or program-matic templates may require a different counterfac-tual prompt construction. Each sample requires |C| + 1 teacher forward passes, which increases per-step computation and can increase memory use and latency relative to single-pass OPD. Section 4.4 reports lower total FLOPs at matched accuracy on the two evalu-ated model pairs. The additional per-step over-head remains relevant for memory- or throughput-constrained training systems. Finally, the self-distillation comparison in Ta-ble 1 shows a clear average gain for self-CC-OPD on the Qwen3-4B pair but only a small average difference from DR-CFG on the Qwen2.5-1.5B pair. With only these two model settings, the com-parison is insufficient to establish whether the use-fulness of the LOO signal changes systematically with teacher scale. Characterizing this relationship across a wider range of teacher sizes is an open direction. 

References 

Rishabh Agarwal, Nino Vieillard, Yongchao Zhou, Pi-otr Stanczyk, Sabela Ramos Garea, Matthieu Geist, and Olivier Bachem. 2024. On-policy distillation of language models: Learning from self-generated mistakes. In International Conference on Learning Representations .Geoffrey Cideron, Andrea Agostinelli, Johan Ferret, Ser-tan Girgin, Romuald Elie, Olivier Bachem, Sarah Per-rin, and Alexandre Rame. 2025. Diversity-rewarded CFG distillation. In International Conference on Learning Representations .Junfeng Fang, Zhepei Hong, Mao Zheng, Mingyang Song, Gengsheng Li, Houcheng Jiang, Dan Zhang, Haiyun Guo, Xiang Wang, and Tat-Seng Chua. 2026. Rubric-based on-policy distillation. Preprint ,arXiv:2605.07396. Yuxian Gu, Li Dong, Furu Wei, and Minlie Huang. 2024. MiniLLM: Knowledge distillation of large language models. In International Conference on Learning Representations .Anisha Gunjal, Anthony Wang, Elaine Lau, Vaskar Nath, Yunzhong He, Bing Liu, and Sean Hendryx. 2025. Rubrics as rewards: Reinforcement learning beyond verifiable domains. Preprint ,arXiv:2507.17746. Geoffrey E. Hinton. 2002. Training products of experts by minimizing contrastive divergence. Neural Com-putation , 14(8):1771–1800. Yuxin Jiang, Yufei Wang, Xingshan Zeng, Wanjun Zhong, Liangyou Li, Fei Mi, Lifeng Shang, Xin Jiang, Qun Liu, and Wei Wang. 2024. Follow-Bench: A multi-level fine-grained constraints follow-ing benchmark for large language models. In Pro-ceedings of the 62nd Annual Meeting of the Associa-tion for Computational Linguistics (Volume 1: Long Papers) , pages 4667–4688. Association for Compu-tational Linguistics. Woogyeol Jin, Taywon Min, Yongjin Yang, Dennis Wei, Yi Zhou, Swanand Ravindra Kadhe, Nathalie Baracaldo, and Kimin Lee. 2026. Entropy-aware on-policy distillation of language models. In Pro-ceedings of the 43rd International Conference on Machine Learning , volume 306 of Proceedings of Machine Learning Research . PMLR. Jongwoo Ko, Sungnyun Kim, Tianyi Chen, and Se-Young Yun. 2024. DistiLLM: Towards streamlined distillation for large language models. In Proceed-ings of the 41st International Conference on Machine Learning , volume 235 of Proceedings of Machine Learning Research , pages 24872–24895. PMLR. Xiang Lisa Li, Ari Holtzman, Daniel Fried, Percy Liang, Jason Eisner, Tatsunori Hashimoto, Luke Zettle-moyer, and Mike Lewis. 2023. Contrastive decod-ing: Open-ended text generation as optimization. In 

Proceedings of the 61st Annual Meeting of the As-sociation for Computational Linguistics (Volume 1: Long Papers) , pages 12286–12312. Association for Computational Linguistics. Yaxuan Li, Yuxin Zuo, Bingxiang He, Jinqian Zhang, Chaojun Xiao, Cheng Qian, Tianyu Yu, Huan-ang Gao, Wenkai Yang, Zhiyuan Liu, and Ning Ding. 2026. Rethinking on-policy distillation of large lan-guage models: Phenomenology, mechanism, and recipe. Preprint , arXiv:2604.13016. Hao Peng, Yunjia Qi, Xiaozhi Wang, Bin Xu, Lei Hou, and Juanzi Li. 2025. VerIF: Verification engineering for reinforcement learning in instruction following. In Proceedings of the 2025 Conference on Empiri-cal Methods in Natural Language Processing , pages 30324–30339. Association for Computational Lin-guistics. Valentina Pyatkin, Saumya Malik, Victoria Graf, Hamish Ivison, Shengyi Huang, Pradeep Dasigi, Nathan Lambert, and Hannaneh Hajishirzi. 2025. Generalizing verifiable instruction following. In Ad-vances in Neural Information Processing Systems Datasets and Benchmarks Track .Yunjia Qi, Hao Peng, Xiaozhi Wang, Bin Xu, Lei Hou, and Juanzi Li. 2025. Constraint back-translation im-proves complex instruction following of large lan-guage models. In Proceedings of the 34th ACM Inter-national Conference on Information and Knowledge Management , pages 2388–2398. ACM. Yiwei Qin, Kaiqiang Song, Yebowen Hu, Wenlin Yao, Sangwoo Cho, Xiaoyang Wang, Xuansheng Wu, Fei Liu, Pengfei Liu, and Dong Yu. 2024. InFoBench: Evaluating instruction following ability in large lan-guage models. In Findings of the Association for Computational Linguistics: ACL 2024 , pages 13025– 13048. Association for Computational Linguistics. Qwen Team, An Yang, Baosong Yang, Beichen Zhang, Binyuan Hui, Bo Zheng, Bowen Yu, Chengyuan Li, Dayiheng Liu, Fei Huang, Haoran Wei, Huan Lin, Jian Yang, Jianhong Tu, Jianwei Zhang, Jianxin Yang, Jiaxi Yang, Jingren Zhou, Junyang Lin, and 24 others. 2024. Qwen2.5 technical report. Preprint ,arXiv:2412.15115. Gian-Carlo Rota. 1964. On the foundations of com-binatorial theory I. theory of Möbius functions. 

Zeitschrift für Wahrscheinlichkeitstheorie und Ver-wandte Gebiete , 2(4):340–368. Guillaume Sanchez, Alexander Spangher, Honglu Fan, Elad Levi, and Stella Biderman. 2024. Stay on topic with classifier-free guidance. In Proceedings of the 41st International Conference on Machine Learning ,volume 235 of Proceedings of Machine Learning Research , pages 43197–43234. PMLR. John Schulman, Filip Wolski, Prafulla Dhariwal, Alec Radford, and Oleg Klimov. 2017. Prox-imal policy optimization algorithms. Preprint ,arXiv:1707.06347. Zhihong Shao, Peiyi Wang, Qihao Zhu, Runxin Xu, Junxiao Song, Xiao Bi, Haowei Zhang, Mingchuan Zhang, Y. K. Li, Y. Wu, and Daya Guo. 2024. DeepSeekMath: Pushing the limits of mathemati-cal reasoning in open language models. Preprint ,arXiv:2402.03300. Guangming Sheng, Chi Zhang, Zilingfeng Ye, Xibin Wu, Wang Zhang, Ru Zhang, Yanghua Peng, Haibin Lin, and Chuan Wu. 2025. HybridFlow: A flexible and efficient RLHF framework. In Proceedings of the Twentieth European Conference on Computer Systems , pages 1279–1297. ACM. Weijia Shi, Xiaochuang Han, Mike Lewis, Yulia Tsvetkov, Luke Zettlemoyer, and Wen-tau Yih. 2024. Trusting your evidence: Hallucinate less with context-aware decoding. In Proceedings of the 2024 Confer-ence of the North American Chapter of the Associ-ation for Computational Linguistics: Human Lan-guage Technologies (Volume 2: Short Papers) , pages 783–791. Association for Computational Linguistics. Vijay Viswanathan, Yanchao Sun, Xiang Kong, Meng Cao, Graham Neubig, and Sherry Wu. 2025. Check-lists are better than reward models for aligning lan-guage models. In Advances in Neural Informa-tion Processing Systems , volume 38, pages 114728– 114754. Curran Associates, Inc. Bosi Wen, Pei Ke, Xiaotao Gu, Lindong Wu, Hao Huang, Jinfeng Zhou, Wenchuang Li, Binxin Hu, Wendy Gao, Jiaxin Xu, Yiming Liu, Jie Tang, Hongn-ing Wang, and Minlie Huang. 2024. Benchmark-ing complex instruction-following with multiple con-straints composition. In Advances in Neural Informa-tion Processing Systems , volume 37, pages 137610– 137645. Curran Associates, Inc. Tianze Xu, Yanzhao Zheng, Pengrui Lu, Lyuman-shan Ye, Yong Wu, Zhentao Zhang, Yuanqiang Yu, Chao Ma, Jihuai Zhu, Pengfei Liu, Baohua Dong, Hangcheng Zhu, Ruohui Huang, and Gang Yu. 2026. Rubrics to tokens: Bridging response-level rubrics and token-level rewards in instruction following tasks. 

Preprint , arXiv:2604.02795. An Yang, Anfeng Li, Baosong Yang, Beichen Zhang, Binyuan Hui, Bo Zheng, Bowen Yu, Chang Gao, Chengen Huang, Chenxu Lv, Chujie Zheng, Day-iheng Liu, Fan Zhou, Fei Huang, Feng Hu, Hao Ge, Haoran Wei, Huan Lin, Jialong Tang, and 41 others. 2025. Qwen3 technical report. Preprint ,arXiv:2505.09388. Wenkai Yang, Weijie Liu, Ruobing Xie, Kai Yang, Saiy-ong Yang, and Yankai Lin. 2026. Learning beyond Teacher: Generalized on-policy distillation with re-ward extrapolation. Preprint , arXiv:2602.12125. Junjie Ye, Caishuang Huang, Zhuohan Chen, Wenjie Fu, Chenyuan Yang, Leyi Yang, Yilong Wu, Peng Wang, Meng Zhou, Xiaolong Yang, Tao Gui, Qi Zhang, Zhongchao Shi, Jianping Fan, and Xuanjing Huang. 2025. MulDimIF: A multi-dimensional constraint framework for evaluating and improving instruc-tion following in large language models. Preprint ,arXiv:2505.07591. Kongcheng Zhang, Qi Yao, Shunyu Liu, Wenjian Zhang, Min Cen, Yang Zhou, Wenkai Fang, Yiru Zhao, Baisheng Lai, and Mingli Song. 2025a. Replay failures as successes: Sample-efficient reinforce-ment learning for instruction following. Preprint ,arXiv:2512.23457. Songming Zhang, Xue Zhang, Tong Zhang, Bojie Hu, Yufeng Chen, and Jinan Xu. 2025b. AlignDistil: Token-level language model alignment as adaptive policy distillation. In Proceedings of the 63rd An-nual Meeting of the Association for Computational Linguistics (Volume 1: Long Papers) , pages 19791– 19807. Association for Computational Linguistics. Tao Zhang, Chenglin Zhu, Yanjun Shen, Wenjing Luo, Yan Zhang, Hao Liang, Tao Zhang, Fan Yang, Min-gan Lin, Yujing Qiao, Weipeng Chen, Bin Cui, Wen-tao Zhang, and Zenan Zhou. 2025c. CFBench: A comprehensive constraints-following benchmark for LLMs. In Proceedings of the 63rd Annual Meet-ing of the Association for Computational Linguistics (Volume 1: Long Papers) , pages 32926–32944. Asso-ciation for Computational Linguistics. Jeffrey Zhou, Tianjian Lu, Swaroop Mishra, Siddhartha Brahma, Sujoy Basu, Yi Luan, Denny Zhou, and Le Hou. 2023. Instruction-following evaluation for large language models. Preprint , arXiv:2311.07911. Yang Zhou, Sunzhu Li, Shunyu Liu, Wenkai Fang, Kongcheng Zhang, Jiale Zhao, Jingwen Yang, Yihe Zhou, Jianwei Lv, Tongya Zheng, Hengtong Lu, Wei Chen, Yan Xie, and Mingli Song. 2025. Breaking the exploration bottleneck: Rubric-scaffolded reinforce-ment learning for general LLM reasoning. Preprint ,arXiv:2508.16949. A Implementation and Evaluation Details 

This appendix details the hyperparameters, datasets, baselines, and evaluation protocol referenced by the main paper. Algorithm 1 gives the complete CC-OPD training step in a form that can be dropped into an existing sampled-token OPD trainer with no change to the rollout or PPO modules. 

A.1 Training Hyperparameters for OPD-Family Methods 

The base configuration in Table 4, implemented in the verl (Sheng et al., 2025) framework, is used by CC-OPD, the vanilla OPD baseline, and the three OPD-augmentation variants. ExOPD, Teacher-TopK RKL, and Teacher-TopK SRKL use the same training corpus, optimizer, batching, roll-out settings, and hardware as CC-OPD, differ-ing only in their training losses and the corre-sponding loss-specific hyperparameters reported in §4.1 and Table 1. ExOPD retains the sampled-token PPO update, whereas Teacher-TopK RKL and Teacher-TopK SRKL optimize direct losses over the teacher’s normalized top-32 support; PPO-specific reward and advantage entries therefore do not apply to the two Teacher-TopK controls. Both the Qwen2.5-1.5B and Qwen3-4B student pairs use identical settings; per-step compute differs because the model sizes differ across pairs. PPO advan-tage estimation uses the OPD branch (no critic, no GAE), and the actor is updated for a single PPO epoch per mini-batch. Each verl-step sam-ples 128 prompts × 8 rollouts = 1 ,024 trajectories and triggers 8 gradient updates (mini-batch 128 ,micro-batch 4 per GPU); we therefore report re-sults on the gradient-step axis using the conversion 

1 verl-step = 8 gradient steps. CC-OPD is trained for 1,500 verl-steps; the vanilla OPD baseline and the three OPD-augmentation variants are trained for 6,000 verl-steps. We report each method at its saturated checkpoint: step 1,500 for CC-OPD and step 3,000 for the four baselines. This asymmetric training horizon is a saturation check rather than a budget asymmetry that favors CC-OPD: CC-OPD plateaus on validation MulDimIF by step 1,500 

while the baselines plateau by step 3,000 , and we train the baselines through step 6,000 to confirm no further improvement (the full 48 ,000 -gradient-step baseline trajectory is shown in §4.4). These methods use 32 GPUs (96 GB per GPU) in a single Ray cluster. 

Algorithm 1 One CC-OPD training step.                                                                                                     

> Require: Student πθ, frozen teacher PT, mini-batch B, shap-ing strength λ, clip threshold c
> 1: for (q, C, inst (q, C )) ∈Bdo
> 2: y∼πθ(· | q, C ); cache log πθ(yt|q, C, y <t ),t=1, . . . , L ▷student rollout + log-probs 3: ℓ(C;t)←log PT(yt|q, C, y <t ),t= 1 , . . . , L ▷
> full-context teacher pass 4: cKL( t)←log πθ(yt|q, C, y <t )−ℓ(C;t),t=1, . . . , L ▷Eq. (2) 5: for i= 1 , . . . , |C|do
> 6: q′
> i←inline_remove (inst (q, C ), c i)
> 7: ℓ(C\{ ci};t)←log PT(yt|q′
> i, y <t )
> 8: δi(t)←ℓ(C;t)−ℓ(C\{ ci};t)▷Eq. (4) 9: end for
> 10: ˆδ(t)←clip  P
> iδi(t),−c, +c▷Eq. (5)–(6) 11: rt← − cKL( t) + λˆδ(t)▷Eq. (7) 12: end for
> 13: Update πθwith ratio-clipped PPO on {rt, y, q, C }B

A.2 HIR-16K Training Corpus 

We train all compared methods, including the GRPO baselines, on HIR-16K (Zhang et al., 2025a), an instruction-following corpus contain-ing 16 ,968 samples with multi-constraint specifi-cations. Each sample is a tuple (q, C, V) where 

q is a base prompt, C = {c1, . . . , c N } is its con-straint set, and V is the constraint-verification meta-data. Constraint cardinality |C| ranges from 5

to 17 (mean 7.25 , median 7). Constraints fall into three verifier categories: rule-based atomic constraints checkable with deterministic code (ifeval_like , 6,998 samples), table-structured rule constraints ( constraint_table , 3,608 sam-ples), and free-text constraints requiring an LLM judge ( llm_checker , 6,362 samples). 

Exact-match audit. After Unicode NFKC, case, and whitespace normalization, no evaluation prompt exactly matches either a HIR base prompt or a full rendered training instruction. 

A.3 Teacher Training and GRPO Baselines GRPO-IR teachers. The Qwen2.5-7B and Qwen3-4B teachers are obtained by GRPO (Shao et al., 2024) fine-tuning of the corresponding In-struct base with an instruction-level accuracy re-ward ( GRPO-IR , matching RL-IR of Peng et al. (2025); Pyatkin et al. (2025)): r = 1 iff every con-straint ci ∈ C is satisfied, else r = 0 . The GRPO-CR baseline (matching RL-CR of Qi et al. (2025); Pyatkin et al. (2025)) replaces this with a constraint-level mean reward r = 1

> |C|

P 

> i

1[ci satisfied ].The training implementation strictly follows Xu et al. (2026). Per-constraint satisfaction is deter-Hyperparameter Value                              

> PPO
> Clip ratio ϵ0.2
> PPO epochs / mini-batch 1
> Optimizer
> Optimizer AdamW Actor LR 2×10 −6
> Betas (β1, β 2)(0 .9,0.999)
> Weight decay 0.01
> LR schedule constant
> Batching
> Prompts per verl-step 128
> Rollouts per prompt 8
> PPO mini-batch 128
> Micro-batch per GPU 4
> Rollout (vLLM)
> Temperature / top-p/ top-k1.0/1.0/−1
> Max prompt / response length 2048 /1024
> CC-OPD-specific
> Shaping strength λ2.0
> Clip threshold c5.0
> Constraint subsample ratio ρ1.00

Table 4: Base training configuration for the OPD-family methods. The final CC-OPD-specific block applies only to CC-OPD. ExOPD and the two Teacher-TopK controls share the optimizer, batching, rollout, and hardware settings but use the objective-specific losses described in §4.1. PPO-specific entries do not apply to the direct-loss Teacher-TopK controls. 

mined by combining the original benchmark ver-ifiers: rule-based constraints ( ifeval_like and 

constraint_table subsets) are checked by the IFEval-style code verifier, and free-text constraints (llm_checker subset) are scored by DeepSeek-V3.1 1 as the unified judge. The shared GRPO actor configuration is summarized in Table 5. GRPO uses no KL penalty; group-normalized advantages and asymmetric PPO clipping are applied as in the standard GRPO recipe. 

RTT-GRPO. RTT-GRPO follows the original implementation and method-specific training hy-perparameters of Xu et al. (2026) without modifica-tion, while using the model pairs, HIR-16K training corpus, and evaluation protocol of this paper. 

Teacher checkpoint convergence. For the Qwen2.5-7B GRPO-IR teacher, 25 validation eval-uations cover steps 20–500. At steps 420, 440, 460, 480, and 500, the valid-sample strict aver-ages over IFEval, IFBench, and MulDimIF are 

0.6124 , 0.6088 , 0.6040 , 0.6102 , and 0.6090 , re-                           

> 1https://huggingface.co/deepseek-ai/ DeepSeek-V3.1
> Hyperparameter Value
> Group size (rollouts per prompt) 8
> PPO epochs 1
> PPO clip (low / high) 0.2/0.27
> rollout batch size 64
> n rollout 8
> ppo batch size 256
> Advantage normalization per-group mean and std KL coefficient 0(no reference) Reward whitening off Actor LR 1×10 −6
> Warmup steps 20
> Weight decay 0
> Rollout temp. / top-p/ top-k0.99 /0.99 /100
> Prompt / response length 2048 /2048
> Max training steps 500
> Precision bf16

Table 5: GRPO hyperparameters shared between GRPO-IR and GRPO-CR. The two recipes differ only in the reward aggregation function. 

spectively (mean 0.6089 ); the late-stage slopes on all three benchmarks are approximately zero. The Qwen3-4B GRPO-IR teacher is likewise evaluated 25 times from steps 20–500. Its final checkpoint is only 0.0055 , 0.0034 , and 0.0092 below the re-spective IFEval, IFBench, and MulDimIF trajec-tory peaks. These trajectories indicate a plateau or near-saturation under the evaluated recipes, but do not rule out improvement from a different GRPO recipe. 

SFT baseline. The SFT baseline fine-tunes each student on greedy teacher-generated responses to all HIR-16K prompts. We use AdamW with learn-ing rate 10 −5, batch size 128 , three epochs, and FSDP-2 sharding across 32 GPUs. 

Self-distillation baselines. In the Self-Distillation Comparison block of Table 1, the external teacher is replaced by the student’s own frozen initialization weights and the same PPO machinery is used otherwise. DR-CFG 

reproduces the core CFG-distillation objective of Cideron et al. (2025), who form a guided teacher distribution by linearly combining conditional and unconditional logits with guidance strength 

γ = 3 and minimize the KL from the student to a softmax over the guided logits. Because our framework operates on a sampled-token OPD estimator rather than the full-vocabulary KL objective of the original paper, we adapt this objective to a sampled-token estimator. At each realized student token we compute the conditional-vs-unconditional log-probability difference under the frozen self-initialization, 

log PT (yt | q, C, y <t ) − log PT (yt | q, y <t ), scale it by γ − 1 = 2 , and add the symmetrically clipped result to the vanilla sampled-token OPD reward. This adaptation matches the log-ratio direction and ignores the full-vocabulary normalizer term that drops out of the sampled estimator. The diversity-reward component of the original paper is omitted because it targets text-to-music generation and is not directly applicable in our text-instruction domain. self-CC-OPD applies CC-OPD’s reward shaping (Eq. (7)) to the self-distillation setting by replacing the external teacher in Eq. (4) with the frozen self-initialization. 

Per-benchmark breakdown. On Qwen3-4B, self-CC-OPD averages 61 .9 across the seven bench-marks, compared with 61 .0 for DR-CFG. It scores higher on IFEval, IFBench, MulDimIF, Com-plexBench, and InfoBench, ties on FollowBench, and is 0.1 pp lower on CFBench. On Qwen2.5-1.5B, the corresponding averages are 37 .7 and 37 .5.Self-CC-OPD is 7.7 pp higher on IFEval and also scores higher on MulDimIF and ComplexBench; DR-CFG scores higher on the other four bench-marks. 

A.4 OPD Data-Augmentation Baselines 

The three OPD-augmentation baselines train vanilla sampled-token OPD on a HIR-16K cor-pus that has been enlarged by synthetically rewrit-ing each row’s constraint set. From a row 

(q, C, y teacher ) with |C| = N , each augmentation emits N additional rows according to a different rule: • OPD-aug-LOO : the i-th new row keeps C \{ci} (one constraint removed at a time). • OPD-aug-Rand : each new row samples a subset of size uniformly drawn from 1 to N −1

and keeps the corresponding random comple-ment. • OPD-aug-SC : the i-th new row keeps only 

{ci} (one constraint at a time). Augmented rows are concatenated with the original 

16 ,968 rows; the training objective and PPO config-uration are identical to vanilla sampled-token OPD with no counterfactual reward shaping. Because the augmented corpus is roughly 1 + ¯N ≈ 8×

larger (where ¯N ≈ 7.25 is the mean constraint count), these baselines need substantially more gra-dient updates to traverse the data; we train them to 

6,000 verl-steps and report the step-3,000 check-point where the validation curve has plateaued. 

A.5 Counterfactual Variant Ablations 

§4.5 compared three alternative counterfactual schemes to the main LOO construction of CC-OPD. All four variants share λ = 2 .0, clip threshold 

c = 5 .0, and constraint subsample ratio ρ = 1 .00 ;they differ only in which counterfactual prompts 

A are constructed and how the per-token signals 

δ(t) = ℓC (t) − ℓA(t) are aggregated. The same re-alized student rollout y is shared across all teacher forward passes within a variant; only the prompt context varies. • LOO (main): for each constraint ci, rescore the teacher under Ai = C \ { ci}. Per-token signal δi(t) = ℓC (t) − ℓC\{ ci}(t); aggregate by δ(t) = P 

> i

δi(t).• LA (leave-all) : a single full-vs-empty con-trast. Rescore the teacher under the empty con-straint context A = ∅; δ(t) = ℓC (t) − ℓ∅(t).• RSS (random subset) : for each of |C| jobs, sample a removed-subset size uniformly from 

1 to |C|− 1, draw the removed indices without replacement, and rescore under Aj = C \ Sj .Aggregate by δ(t) = P 

> j

δj (t).• NAO (No-context Anchor Only) : for each 

ci, rescore the teacher both under the single-ton A+ 

> i

= {ci} and under the empty context 

A0 = ∅ (the latter is shared across all i). Per-token signal δi(t) = ℓ{ci}(t) − ℓ∅(t); aggre-gate by δ(t) = P 

> i

δi(t).LA isolates the effect of constraint presence as a single bulk contrast. RSS preserves the multi-constraint structure of LOO but blurs the attribution to any individual constraint. NAO replaces the full-context anchor with a no-context anchor, asking how each singleton conditioning shifts likelihood relative to no conditioning at all. LOO is the only variant whose contrasts vary by exactly one named constraint. 

A.6 Evaluation Protocol Inference. For every evaluated model we gen-erate responses with vLLM using T = 0 .6, top-

p = 1 .0, max_tokens = 4096 , and n = 3 samples per prompt. The reported number is the arithmetic mean across the three samples. Samples whose judge call fails to parse on all three attempts are excluded from scoring. Rule-based scoring. IFEval, IFBench, and MulDimIF are scored by deterministic verifiers from their official evaluation scripts. We report strict prompt accuracy for IFEval and IFBench and overall accuracy averaged over the four difficulty levels for MulDimIF. 

LLM-judged scoring. ComplexBench, In-foBench, FollowBench, and CFBench rely on an LLM judge for each atomic criterion. The original judges specified by these benchmarks (legacy gpt-4-1106 and gpt-4-0314 ) have been deprecated by OpenAI; to keep scoring consistent across all evaluated models and to avoid drift from per-benchmark judge changes, we adopt a unified contemporary judge: gpt-5.4-mini-0317 

for ComplexBench, InfoBench, and FollowBench, and gpt-4o-0513 for CFBench (whose original judge is still active). All judge prompt templates and aggregation rules follow the official scorer of each benchmark. 

B Structural Relationship between LOO Sum and LA: a Möbius Decomposition 

Section 4.5 summarizes the structural difference be-tween the per-constraint leave-one-out aggregation P 

> i

δLOO ,i and the single leave-all contrast δLA =log PT (yt | q, C, y <t ) − log PT (yt | q, ∅, y <t ).Both reduce to 1 × T token-level vectors before entering the OPD update, but they apply different weights to the same underlying set-function inter-actions. This appendix gives the full derivation, extends it to NAO and RSS, and reports additional diagnostics and caveats. Strict additivity of the teacher’s constraint response is sufficient for LOO and LA to agree; equality at a single token does not imply additivity because signed higher-order terms can cancel. 

B.1 Setup: the teacher log-probability as a set function 

For a token yt in a realized student rollout, fix the query q, the prefix y<t , and consider only how the teacher’s log-probability of yt depends on the conditioning constraint subset S ⊆ C. Define 

ft(S) := log PT

 yt q, S, y <t 

. (9) Then ft is a real-valued function on the Boolean lattice 2C ordered by inclusion. The two estimators we compare are 

δLA (t) = ft(C) − ft(∅), (10) 

δLOO (t) = 

> N

X

> i=1

h

ft(C) − ft

 C \{ ci}i

. (11) We suppress the token index t in the rest of this section; all derivations are pointwise per-token. 

B.2 Möbius decomposition of f

The Möbius inversion theorem for the Boolean lat-tice (Rota, 1964) states that any real-valued func-tion f on 2C admits a unique decomposition 

f (S) = X 

> T⊆S

m(T ),m(T ) = X 

> U⊆T

(−1) |T |−| U | f (U ). (12) The coefficient m(T ) has a natural interpretation as the order-|T | irreducible interaction of the con-straints in T :• m(∅) = f (∅), the unconditioned log-probability; • m({ci}) = f ({ci}) − f (∅), the first-order main effect of constraint ci alone; • m({ci, c j }) = f ({ci, c j }) − f ({ci}) −

f ({cj }) + f (∅), the second-order pairwise interaction between ci and cj that is not ex-plained by either single-constraint effect; • more generally, m(T ) captures the joint effect of the constraints in T that no strict subset can account for. The teacher’s conditioning function is additive in constraints if and only if m(T ) = 0 for every 

|T | ≥ 2. In that case, each constraint contributes a fixed log-probability shift independent of the oth-ers, and no multi-constraint interaction exists. 

B.3 LA and LOO sum as different weighted aggregations 

Applying Eq. (12) to LA: 

δLA = f (C) − f (∅)= X 

> T⊆C

m(T ) − m(∅)= X 

> ∅̸ =T⊆C

m(T ).

(13) LA is thus the uniformly weighted sum over all non-empty Möbius coefficients. Applying the inversion to each leave-one-out term: 

δLOO ,i = f (C)−f (C\{ ci}) = X 

> T⊆C, c i∈T

m(T ).

(14) Each δLOO ,i collects exactly those Möbius coeffi-cients whose subset T contains ci. Summing over 

i:

δLOO =

> N

X

> i=1

δLOO ,i 

= X 

> T⊆C

m(T ) · |{ i : ci ∈ T }| 

= X 

> ∅̸ =T⊆C

|T | · m(T ).

(15) LOO sum is therefore the cardinality-weighted sum over the same non-empty Möbius coefficients. The two estimators are aligned on each subset T but weight it differently: LA assigns weight 1, while LOO sum assigns weight |T |.The residual is a clean signature of the higher-order interactions: 

δLA − δLOO = X 

> ∅̸ =T⊆C

 1 − | T | m(T )= − X 

> |T|≥ 2

 |T | − 1 · m(T ). (16) Three properties follow immediately. (i) Singleton main effects m({ci}) contribute equally to both LA and LOO sum and therefore drop out of Eq. (16). (ii) A nonzero residual certifies that at least one higher-order coefficient is nonzero, whereas a zero residual does not rule out such interactions because signed terms can cancel. (iii) Coefficient by coeffi-cient, LOO assigns an order-k interaction k times the weight assigned by LA; this does not imply that the aggregate residual must grow monotonically with interaction order because the coefficients are signed and the production reward is clipped. 

NAO and RSS in the same framework. The remaining two variants from §4.5 also admit clean Möbius expansions. NAO computes δNAO =P

> i

[f ({ci}) − f (∅)] = P  

> |T|=1

m(T ), retaining only the order-1 main effects and discarding every higher-order interaction. This is the most aggres-sive truncation among the four variants and is there-fore expected—but not guaranteed—to lose useful signal when higher-order interactions matter; the policy-level ablation in §4.5 tests that expectation. For RSS, let Sj denote the subset removed by job 

j, and let k = |T | for a non-empty Möbius subset 

T . A job includes m(T ) exactly when T ∩ Sj̸ = ∅.Under our sampling rule—first drawing |Sj | uni-formly from {1, . . . , N −1}, then drawing a subset uniformly at that size—the inclusion probability is 

pN,k = 1 − 1

N − 1 

> N−1

X

> s=1

 N −ks

 Ns



= N k − 1(N − 1)( k + 1) .

(17) Because RSS sums N independent jobs, the cu-mulative coefficient weight satisfies W RSS  

> T

∼

Binomial( N, p N,k ), with 

E[W RSS  

> T

] = wN,k = N (N k − 1) (N − 1)( k + 1) . (18) Hence, before clipping, 

E[δRSS ] = X 

> ∅̸ =T⊆C

wN, |T | m(T ). (19) The expected weight increases with interaction or-der but with diminishing increments: wN, 1 = N/ 2,

wN,N = N , and wN,k +1 − wN,k = N (N +1) /[( N − 1)( k + 1)( k + 2)] . Relative to its sin-gleton weight, RSS therefore increases from 1 to 

2, rather than linearly with k. Importantly, the un-normalized N -job RSS sum does not interpolate in absolute scale between LA and LOO: wN,k ≥ k.RSS thus changes both interaction-order weighting and pre-clip reward scale, while also introducing subset-sampling variance. The full four-variant tax-onomy is therefore: LA (uniform weight 1), RSS (the expected weight in Eq. (18), with sampling variance), LOO (cardinality weight |T | determinis-tically), and NAO (order-1 truncation). 

B.4 Empirical validation: the additivity hypothesis is violated 

We measure the empirical residual at scale by forced-scoring the frozen teacher on the same student rollouts under all N +2 contexts {C} ∪ {C \ { ci}} Ni=1 ∪ {∅} . On 15 ,450 HIR-16K sam-ples with |C| ∈ [5 , 10] scored against a Qwen2.5-1.5B-Instruct student rollout under a Qwen2.5-7B teacher, Table 6 reports per-token statistics that summarize the gap. If the teacher response were strictly additive, Eq. (16) would vanish. The observed relative L1Metric Value                               

> Sample size (HIR-16K, |C| ∈ [5 ,10] )15 ,450
> Mean constraint count ¯N6.63
> ∥δLA −δLOO ∥1/∥δLA ∥10.918
> Sign agreement Pr[sgn( δLA ) = sgn( δLOO )] 0.681
> Signed Pearson correlation ρ(δLA , δ LOO )0.665
> Top-20 token Jaccard overlap 0.452
> L2-norm ratio ∥δLOO ∥2/∥δLA ∥21.137
> Table 6: Per-token comparison of the LA and LOO sum estimators on 15 ,450 HIR-16K samples. The rela-tive L1residual measures the aggregate magnitude of P
> |T|≥ 2(|T| − 1) m(T)relative to LA; sign agreement measures the fraction of tokens where the two estima-tors provide shaping signals in the same direction. All statistics are computed before clipping.

residual of 0.918 is nonzero and therefore rejects strict additivity for these measured signals; it quan-tifies the aggregate weighted residual, not the mag-nitudes of individual Möbius coefficients. The signed Pearson correlation of 0.665 and the 31 .9%sign disagreement further show that LA and LOO differ in both scale and direction, while the L2-norm ratio indicates that LOO’s aggregate signal has a 13 .7% larger norm in this sample. Symmetric clipping preserves the sign of nonzero values by construction, so clipping behavior is not treated as independent evidence about whether the residual is driven by outliers. 

B.5 What LOO retains beyond LA: the K ×T

sensitivity map 

While both δLA and δLOO are 1 × T vectors after summation, LOO first constructs an unsummed 

K ×T matrix δLOO ,i (t). By Eq. (14), its i-th row collects the Möbius coefficients of every subset that contains ci, forming a per-token, per-constraint finite-difference sensitivity map. LA’s single full-vs-empty contrast cannot recover this map: with only the two probe contexts {C, ∅} , LA produces one scalar at each token and collapses the entire 

K-dimensional identity structure. Figure 4 reports five summary statistics of the per-token sensitivity map across the same 15 ,450 

samples. The map is concentrated rather than dif-fuse. On average 42 .6% of the per-constraint signal mass at any single token comes from one dominant constraint (vs. 15 .0% expected under uniform at-tribution), and an inverse-participation-ratio esti-mate of “active constraints per token” gives 4.46 

against a nominal mean of 6.63 (67 % participa-tion). The mean Jaccard similarity between pairs of per-constraint top-20 token sets is 0.269 . These five metrics jointly describe a structured, sparse sensitivity map, and none of them can be computed from LA alone. This row-wise concentration and the multi-constraint interactions measured in §B.4 characterize complementary aspects of the signal: the former describes how sensitivity is distributed across constraints at a token, whereas the latter measures non-additivity across conditioning sub-sets. While the K×T matrix is summed to a scalar before entering PPO, this per-constraint structure is what enables LOO’s summed reward to weight higher-order interactions by |T | (§B.3), an option LA’s full-vs-empty contrast does not have. 

B.6 Cross-model comparison of counterfactual variants 

Table 7 compares the four counterfactual variants on IFEval, IFBench, and the MulDimIF Overall and Level-4 splits for both model pairs. For each pair, LOO has the highest score in three of the four cells (IFEval, MulDimIF Overall, and MulDimIF Level-4), while RSS has the highest score on IF-Bench. 

B.7 Theory–empirics bridge and limitations 

Taken together, the derivation and signal measure-ments show that LA and LOO apply different weights to the same interaction coefficients and that, relative to LA, LOO retains a per-constraint sensitivity map before summation. Token-level case studies illustrating these properties on individ-ual HIR-16K samples are presented in Appendix C. This analysis characterizes the reward geometry 

presented to the optimizer, while the policy-level consequences are measured separately in §4.5. The full Möbius expansion has 2N terms and is in-tractable to enumerate beyond very small constraint sets, so LA and LOO are best understood as two tractable projections of an exponentially large in-teraction lattice rather than as reconstructions of the lattice itself. 

Reconciling structural and policy-level views. 

The +0 .9 pp Avg gap between LOO and LA in Ta-ble 2 is much smaller than the 91 .8 % relative L1

residual and 31 .9 % token-level sign disagreement. The two quantities describe different stages of the pipeline: reward geometry does not uniquely deter-mine the policy reached by stochastic, clipped PPO, and benchmark-level metrics compress many token-level differences into a single response score. The Figure 4: Per-constraint sensitivity structure exposed by LOO. Five per-token statistics of the K ×T LOO sensitivity matrix on 15 ,450 HIR-16K samples (Qwen2.5-1.5B student under Qwen2.5-7B teacher). Panels (A– B): histograms of per-token top-1 constraint dominance and per-token normalized entropy. Panel (C): effective constraint count per token versus nominal K. Panel (D): inter-constraint top-20 token Jaccard, measuring how distinctly different constraints localize. Panel (E): per-constraint token coverage at |δ| > 0.5. The summary panel quantifies how each metric departs from the uniform-attribution baseline.                                          

> Qwen2.5-1.5B Qwen3-4B Variant IFEval IFBench MulDimIF All MulDimIF L4 IFEval IFBench MulDim All MulDim L4 LOO 76.0 28.2 72.9 59.4 86.1 37.6 76.2 63.8
> LA 75.0 27.1 71.0 57.1 85.4 34.2 74.6 62.0 RSS 75.5 28.8 72.1 57.7 85.6 38.0 76.0 62.4 NAO 71.2 26.5 66.4 45.3 84.2 30.2 71.4 53.4

Table 7: Cross-model variant comparison on rule-based benchmarks. The four counterfactual variants (LOO/LA/RSS/NAO) are evaluated on IFEval, IFBench, and the MulDimIF Overall (MulDim All) and Level-4

(MulDim L4) splits for both model pairs. Within each pair, all variants share identical hyperparameters ( λ=2 ,

c=5 , ρ=1 .0, 1,500 verl-steps); only the counterfactual structure differs. Bold marks the best score per column. The Qwen2.5 IFEval/IFBench/MulDimIF-Overall columns are a subset of Table 2; the Qwen2.5 LOO/LA Level-4

entries reproduce Table 3, while the RSS/NAO Level-4 entries and all Qwen3 columns are reported here. 

evidence shows that LOO provides a structured per-constraint sensitivity map that LA cannot recover from one full-vs-empty contrast; the magnitude of the resulting policy-level gap remains an empirical outcome of optimization and evaluation. Determin-ing when this signal-level distinction yields a larger policy-level gap remains an empirical question. 

B.8 Idealized implicit target distribution 

Beyond its Möbius decomposition, the CC-OPD reward shape admits an idealized tokenwise inter-pretation. For a fixed decoding state ht and candi-date vocabulary token v, define δi(v, t ) by replac-ing the realized token yt in Eq. (4) with v, and let 

∆c(v, t ) = clip( P 

> i

δi(v, t ), −c, c ). Maximizing the full-expectation objective λ Ev∼q[∆ c(v, t )] −

KL( q ∥ PT ) over q yields 

q∗(v | ht) ∝ PT (v | ht) exp  λ∆c(v, t ), (20) the unique exponential-tilt solution. Its multiplica-tive form can be viewed as a product-of-experts-style target (Hinton, 2002) driven by teacher-side finite differences rather than an external re-ward model. The Möbius decomposition of §B.3 shows that the unclipped sum inside ∆c cardinality-weights the interaction coefficients; replacing it with LA would assign unit rather than cardi-nality weights and would not construct the pre-aggregation K ×T sensitivity map. Equation (20) describes the idealized full-expectation objective; our experiments instead optimize the sampled-token reward with ratio-clipped PPO. 

C Token-Level Reward Heatmap Case Studies 

This appendix presents three representative HIR-16K samples that visualize, at the individual-token level, the structural difference between LOO and LA characterized in Appendix B. Each case shows two complementary views. Section A of each case lists per-constraint attribution snippets . For the highest-magnitude constraints on that sample, we render short continuous-text excerpts where the per-constraint leave-one-out shift δLOO ,i (t) = log PT (yt | q, C, y <t )−log PT (yt | q, C \{ ci}, y <t )

is largest in absolute value. Each token has a colored background reflecting its δLOO ,i (t) value. Red indicates that the leave-one-out context de-creases the teacher log-probability of this token relative to the full constraint set (i.e., constraint ci

boosts this token), blue indicates the opposite, and intensity scales with |δLOO ,i (t)| capped at ±8.0.

Section B of each case shows the same response colored twice using the two 1 × T aggregated signals, namely the LOO sum P 

> i

δLOO ,i (t) (red header) and the LA contrast δLA (t) = log PT (yt |

q, C, y <t ) − log PT (yt | q, ∅, y <t ) (blue header). Direct visual comparison of the two colored ren-derings shows where the two estimators agree on important tokens and, more often, where they di-verge. The three cases were selected by an offline LLM-verified screening procedure described at the end of this appendix. Together they illustrate three qualita-tively distinct ways in which LOO’s per-constraint signal supplies information that LA cannot recover. To accommodate the full-width tabular layout and the flowing color-coded responses, the case content below is set in a single-column block that may span multiple pages. C.1 Case 1: Literal keyword and format-token alignment 

In the first case, the constraint set requires the response to include specific keywords ("Hardware Com-ponents", "Components", "Software Components") and to format the answer as a numbered list with colon-separated descriptions. The per-constraint attribution snippets in Section A below show that δLOO ,i 

for the keyword constraint peaks literally on the tokens Hardware and Components (δ = +11 .25 and 

+8 .57 respectively), and that δLOO ,i for the numbered-list constraint peaks on the colon-newline token 

:\n (δ = +19 .99 ) and on the digit 1 (δ = +16 .54 ). In Section B, LOO sum mirrors these per-constraint peaks. The heaviest red tokens fall on Hardware , Components , :\n , and 1, while the LA rendering of the same response highlights instead a cluster of generic discourse markers ( or , essential , sentence-final periods) and does not surface any of the four literal constraint-relevant tokens within its top-15 . This case exemplifies the cleanest mode of disagreement. When a constraint demands a specific lexical or format token, the LOO signal localizes on that token, whereas LA, lacking access to per-constraint contrasts, settles on a less localized aggregate. 

Sample 15400 K=8 T =480 LOO/LA top-20 Jaccard = 0 .18 

Section A: Per-constraint attribution snippets (■= + δ, ■= − δ, intensity ∝ | δ|, capped at ±8.0)

C1 

|δ|max =11 .6 “Confine word counts to 35 per sentence.” 

. . . data between different parts of the computer . ←- 2 . Random Access Memory ( RAM . . . . . . the overall functionality of the computer . <|im_end|> 

C2 

|δ|max =11 .2 “Be sure to mention "Hardware Components", "Components", "Software Components" in your response to highlight the key elements and add depth.” 

Hardware Components : ←- 1 . Central Processing Unit . . . . . . the overall functionality of the computer . <|im_end|> 

C3 

|δ|max =20 .0 “Individual entries within the lists should be numbered (1., 2., 3., etc.) and followed by a colon and a brief description, as seen in the g. . . ” 

Hardware Components : ←- 1 . Central Processing Unit ( CPU . . . . . . the overall functionality of the computer . <|im_end|> 

Section B: Full response (same tokens shown twice, colored once by LOO sum, once by LA) −

vmax = ±8.0

LOO sum |δ|max =20 .3:

Hardware Components : ←- 1 . Central Processing Unit ( CPU ): The CPU , or the brain of the computer , is the component that executes instructions , performs calculations , and controls the flow of data between different parts of the computer . ←- 2 . Random Access Memory ( RAM ): RAM , or the working memory , is essential for storing data temporarily while the computer is in use . A larger amount of RAM can significantly enhance the computer ’s performance by allowing it to handle more data at once . ←- 3 . Hard Disk Drive ( H DD ) or Solid State Drive ( SS D ): The HDD or SSD is used to store the computer ’s operating system , applications , and user data . A faster drive can improve the computer ’s overall performance . ←- 4 . Graphics Processing Unit ( GPU ): The GPU is responsible for rendering graphical content and is crucial for computer -generated graphics and video games . A strong GPU can greatly enhance the computer ’s gaming performance . ←- 5 . Mother board : The motherboard is the backbone of the computer , connecting all the hardware components . It facilitates communication between the CPU , RAM , and other peripherals .←- Software Components : ←- 1 . Operating System ( OS ): The OS , such as Windows , macOS , or Linux , is the software that manages the computer ’s hardware resources and provides a user interface for interacting with the computer . ←- 2 . Applications : Applications , such as word processors , web browsers , and games , run on top of the OS and provide specific functionality to the user . ←- 3 . Drivers : Drivers are software that enable hardware components to communicate with the operating system . Without drivers , the hardware components may not function properly . ←- 4 . Ant ivirus Software : Ant ivirus software helps protect the computer from malware and other security threats . ←- 5 . Software Updates : Regular software updates ensure the computer remains secure and runs at its best performance . ←- The inter dependence and inter connection between these hardware and software components are critical for the computer ’s optimal performance and functionality . The CPU and RAM work together to process data , while the GPU and motherboard work together to render graphics . The operating system and applications work together to provide a user interface , while the drivers enable hardware components to communicate with the operating system . The antivirus software and software updates ensure the computer remains secure and runs at its best performance . The inter dependence and inter connection between these components can be metaphor ically compared to a complex ecosystem , where each component plays a vital role in the overall functionality of the computer . <|im_end|> 

LA |δ|max =10 .6:

Hardware Components : ←- 1 . Central Processing Unit ( CPU ): The CPU , or the brain of the computer , is the component that executes instructions , performs calculations , and controls the flow of data between different parts of the computer . ←- 2 . Random Access Memory ( RAM ): RAM , or the working memory , is essential for storing data temporarily while the computer is in use . A larger amount of RAM can significantly enhance the computer ’s performance by allowing it to handle more data at once . ←- 3 . Hard Disk Drive ( H DD ) or Solid State Drive ( SS D ): The HDD or SSD is used to store the computer ’s operating system , applications , and user data . A faster drive can improve the computer ’s overall performance . ←- 4 . Graphics Processing Unit ( GPU ): The GPU is responsible for rendering graphical content and is crucial for computer -generated graphics and video games . A strong GPU can greatly enhance the computer ’s gaming performance . ←- 5 . Mother board : The motherboard is the backbone of the computer , connecting all the hardware components . It facilitates communication between the CPU , RAM , and other peripherals .←- Software Components : ←- 1 . Operating System ( OS ): The OS , such as Windows , macOS , or Linux , is the software that manages the computer ’s hardware resources and provides a user interface for interacting with the computer . ←- 2 . Applications : Applications , such as word processors , web browsers , and games , run on top of the OS and provide specific functionality to the user . ←- 3 . Drivers : Drivers are software that enable hardware components to communicate with the operating system . Without drivers , the hardware components may not function properly . ←- 4 . Ant ivirus Software : Ant ivirus software helps protect the computer from malware and other security threats . ←- 5 . Software Updates : Regular software updates ensure the computer remains secure and runs at its best performance . ←- The inter dependence and inter connection between these hardware and software components are critical for the computer ’s optimal performance and functionality . The CPU and RAM work together to process data , while the GPU and motherboard work together to render graphics . The operating system and applications work together to provide a user interface , while the drivers enable hardware components to communicate with the operating system . The antivirus software and software updates ensure the computer remains secure and runs at its best performance . The inter dependence and inter connection between these components can be metaphor ically compared to a complex ecosystem , where each component plays a vital role in the overall functionality of the computer . <|im_end|> 

Per-constraint attribution snippets (Section A) show that each constraint’s δit peaks on tokens semantically aligned with the constraint’s content. Full-response coloring (Section B) shows that LOO sum and LA produce visually distinct attribution patterns despite both being 1 ×T signals. 

C.2 Case 2: LA collapses to structurally meaningless tokens 

The second case shows what LA picks up when the constraint set lacks an obvious dominant constraint for a particular response region. Here the constraint set asks for three bullet points, the literal word barriers 

to appear at least three times, and an explicit postscript starting with P.S. at the end of the response. The per-constraint snippets in Section A show that the postscript constraint’s δLOO ,i peaks specifically on the tokens Post and script (δ = −6.65 and +8 .96 ) at positions 257 –258 in the response, the actual postscript onset. In Section B, LA’s strongest red token in the same response is the end-of-message token 

<|im_end|> , a diffuse end-of-sequence marker that does not point to any specific satisfied constraint. LA’s signal also lights up the very first bullet marker * at position 0 and several mid-response generic words, while LOO sum’s heaviest tokens, by contrast, sit in the actual P.S. region. The two estimators therefore concentrate their signal in nearly disjoint regions of the same response, and the difference cannot be explained by clip threshold or noise. LA does not have access to the per-constraint contrasts that would identify the postscript onset, and its single full-vs-empty contrast spreads support over the response as a whole. Sample 6165 K=7 T =330 LOO/LA top-20 Jaccard = 0 .25 

Section A: Per-constraint attribution snippets (■= + δ, ■= − δ, intensity ∝ | δ|, capped at ±8.0)

C3 

|δ|max =14 .9 “Your answer must contain exactly 3 bullet points. Use the markdown bullet points such as: * This is point 1.” 

* Communication barriers in a workplace can manifest in . . . . . . ings , and technical j argon . ←- * Language differences can arise due to . . . 

C6 

|δ|max =11 .6 “In your response, the word *barriers* should appear at least 3 times.” 

. . . .←- * Cultural misunderstand ings can occur when individuals from different backgrounds communicate with . . . . . . outcomes for their employees and clients . <|im_end|> 

C7 

|δ|max =9 .0 “At the end of your response, please explicitly add a postscript starting with P.S.” 

. . . about different cultural perspectives and values . ←- * Technical j argon can be a . . . . . . , and using appropriate gestures . ←-←- Post script : ←- In conclusion , communication barriers in . . . 

Section B: Full response (same tokens shown twice, colored once by LOO sum, once by LA) −

vmax = ±8.0

LOO sum |δ|max =28 .7:

* Communication barriers in a workplace can manifest in various ways , including language differences , cultural misunderstand ings , and technical j argon . ←-

* Language differences can arise due to the use of different languages or dialect s , which can lead to mis interpret ation of messages . To overcome this barrier , organizations can implement mult ilingual communication tools or provide translation services for employees who speak different languages . ←- * Cultural misunderstand ings can occur when individuals from different backgrounds communicate with each other . To address this , it is essential to foster an inclusive and respectful workplace culture that values diversity and promotes cross -cultural understanding . This can be achieved through training programs that teach employees about different cultural perspectives and values . ←- * Technical j argon can be a significant barrier in professional communication , especially when working in a team with members from diverse backgrounds or in an industry with specialized terminology . To overcome this barrier , organizations should establish clear communication guidelines that define technical terms and ensure that all employees are familiar with them . ←- * Additionally , non -ver bal communication can also be a barrier in the workplace . This can include poor body language , lack of eye contact , or inappropriate gestures . To overcome this barrier , employees should be trained on effective non -ver bal communication techniques , such as maintaining eye contact , using appropriate body language , and using appropriate gestures . ←-←- Post script : ←- In conclusion , communication barriers in the workplace can significantly impact productivity and collaboration . To overcome these barriers , organizations should prioritize creating a diverse and inclusive workplace culture , establishing clear communication guidelines , and providing training on effective communication techniques . By addressing these barriers , organizations can enhance their communication and work efficiency , ultimately leading to better outcomes for their employees and clients . <|im_end|> 

LA |δ|max =20 .9:

* Communication barriers in a workplace can manifest in various ways , including language differences , cultural misunderstand ings , and technical j argon . ←-

* Language differences can arise due to the use of different languages or dialect s , which can lead to mis interpret ation of messages . To overcome this barrier , organizations can implement mult ilingual communication tools or provide translation services for employees who speak different languages . ←- * Cultural misunderstand ings can occur when individuals from different backgrounds communicate with each other . To address this , it is essential to foster an inclusive and respectful workplace culture that values diversity and promotes cross -cultural understanding . This can be achieved through training programs that teach employees about different cultural perspectives and values . ←- * Technical j argon can be a significant barrier in professional communication , especially when working in a team with members from diverse backgrounds or in an industry with specialized terminology . To overcome this barrier , organizations should establish clear communication guidelines that define technical terms and ensure that all employees are familiar with them . ←- * Additionally , non -ver bal communication can also be a barrier in the workplace . This can include poor body language , lack of eye contact , or inappropriate gestures . To overcome this barrier , employees should be trained on effective non -ver bal communication techniques , such as maintaining eye contact , using appropriate body language , and using appropriate gestures . ←-←- Post script : ←- In conclusion , communication barriers in the workplace can significantly impact productivity and collaboration . To overcome these barriers , organizations should prioritize creating a diverse and inclusive workplace culture , establishing clear communication guidelines , and providing training on effective communication techniques . By addressing these barriers , organizations can enhance their communication and work efficiency , ultimately leading to better outcomes for their employees and clients . <|im_end|> 

Per-constraint attribution snippets (Section A) show that each constraint’s δit peaks on tokens semantically aligned with the constraint’s content. Full-response coloring (Section B) shows that LOO sum and LA produce visually distinct attribution patterns despite both being 1 ×T signals. 

C.3 Case 3: Constraint identity at a shared salient token 

The third case shows how the K × T LOO matrix retains constraint identity at a token that is salient under both aggregate signals. The constraint set requires a Markdown table with no more than three rows, but the response contains no table. Ablating the table constraint produces its largest absolute shift on the token These at position 259 (δ = −21 .3). Both the LOO sum and LA assign a strong shift to this token in Section B. The per-constraint LOO view associates the shift with the table constraint, while LA provides the full-versus-empty aggregate. 

Sample 9169 K=5 T =308 LOO/LA top-20 Jaccard = 0 .21 

Section A: Per-constraint attribution snippets (■= + δ, ■= − δ, intensity ∝ | δ|, capped at ±8.0)

C1 

|δ|max =7 .0 “When crafting your response, ensure that it does not exceed 300 words” 

. . . a significant contribution to the field . His research emphasizes the concept of mutually assured . . . . . . reform , and international trade politics . Their research underscores the importance of understanding the . . . 

C3 

|δ|max =8 .8 “Additionally, the response should be written in English with each word capitalized.” 

. . . ←-←- Dr . Don Cas ler ’s work in nuclear disarm ament is fascinating and . . . . . . War . ←-←- On the other hand , exploring other professors ’ work in the areas . . . 

C5 

|δ|max =21 .3 “and a table with no more than 3 rows, clearly integrating these elements into your answer.” 

. . . States in shaping global economic policies . ←-←- These professors , along with Dr . Cas . . . . . . to inform future policies and actions . <|im_end|> 

Section B: Full response (same tokens shown twice, colored once by LOO sum, once by LA) −

vmax = ±8.0

LOO sum |δ|max =25 .1:

# Nuclear Dis arm ament Research ←-←- Dr . Don Cas ler ’s work in nuclear disarm ament is fascinating and has been a significant contribution to the field . His research emphasizes the concept of mutually assured destruction , which serves as a powerful deterrent against armed conflict . Cas ler ’s essay in AP World History del ved into the Cold War era , where the fear of nuclear ann ihilation shaped global politics . This perspective on nuclear deter rence is crucial for understanding the dynamics of international relations during the Cold War . ←-←- On the other hand , exploring other professors ’ work in the areas of land reform or international trade politics can provide a broader perspective on U .S . institutions and policies . Prof ess ors from the University of Illinois at Urb ana -Ch ampaign ( UI UC ) have made substantial contributions to these fields . For instance , Prof . John Doe , an expert in land reform , has published extensively on the history and impacts of land reforms in developing countries . His research highlights the role of land reform in economic development and social justice . ←-←- Prof . Jane Smith , an authority on international trade politics , has also contributed significantly to the academic discourse on global economic systems and international trade policies . Her work often focuses on the historical context of international trade agreements , the impact of globalization , and the role of the United States in shaping global economic policies . ←-←- These professors , along with Dr . Cas ler , offer valuable insights into the historical and contemporary aspects of nuclear disarm ament , land reform , and international trade politics . Their research underscores the importance of understanding the past to inform future policies and actions . <|im_end|> LA |δ|max =9 .9:

# Nuclear Dis arm ament Research ←-←- Dr . Don Cas ler ’s work in nuclear disarm ament is fascinating and has been a significant contribution to the field . His research emphasizes the concept of mutually assured destruction , which serves as a powerful deterrent against armed conflict . Cas ler ’s essay in AP World History del ved into the Cold War era , where the fear of nuclear ann ihilation shaped global politics . This perspective on nuclear deter rence is crucial for understanding the dynamics of international relations during the Cold War . ←-←- On the other hand , exploring other professors ’ work in the areas of land reform or international trade politics can provide a broader perspective on U .S . institutions and policies . Prof ess ors from the University of Illinois at Urb ana -Ch ampaign ( UI UC ) have made substantial contributions to these fields . For instance , Prof . John Doe , an expert in land reform , has published extensively on the history and impacts of land reforms in developing countries . His research highlights the role of land reform in economic development and social justice . ←-←- Prof . Jane Smith , an authority on international trade politics , has also contributed significantly to the academic discourse on global economic systems and international trade policies . Her work often focuses on the historical context of international trade agreements , the impact of globalization , and the role of the United States in shaping global economic policies . ←-←- These professors , along with Dr . Cas ler , offer valuable insights into the historical and contemporary aspects of nuclear disarm ament , land reform , and international trade politics . Their research underscores the importance of understanding the past to inform future policies and actions . <|im_end|> 

Per-constraint attribution snippets (Section A) show that each constraint’s δit peaks on tokens semantically aligned with the constraint’s content. Full-response coloring (Section B) shows that LOO sum and LA produce visually distinct attribution patterns despite both being 1 ×T signals. C.4 Synthesis, selection methodology, and caveats 

The three cases together illustrate a three-step lad-der of LOO behavior that LA cannot replicate. In Case 1, LOO localizes constraint satisfaction at the literal token level. In Case 2, LOO localizes constraint satisfaction at the structural region level while LA collapses to a vacuous EOS token. In Case 3, LOO localizes constraint violations , identi-fying the response region where the student failed to comply, a diagnostic capability LA does not have because it sees only the full-vs-empty aggregate. Each rung of the ladder exercises a different aspect of the K × T attribution matrix described in Ap-pendix B.5, namely literal keyword peaks, regional concentration, and violation-region marking. The cases were selected from a candidate bank of 

169 HIR-16K samples that satisfied a strict filter on LOO/LA divergence (top-20 token Jaccard ≤ 0.30 ,sign disagreement ≥ 35 %, and residual L1 at least equal to the L1 magnitude of the LA signal itself). From this filtered pool, 80 samples were uniformly drawn across constraint count K ∈ [5 , 10] and inspected by four parallel LLM-based evaluation agents, with each agent scoring every candidate on (a) per-constraint semantic alignment of δLOO ,i 

peaks and (b) the degree to which LA top tokens depart from the per-constraint winners, with the latter axis weighted 4×. The three cases shown above were the highest-scoring representatives of three qualitatively distinct LOO-vs-LA disagree-ment modes. The remaining high-scoring candi-dates exhibit similar patterns within these modes (e.g., additional code-domain and prefix-sentence variants). Two caveats are warranted. First, the visualiza-tion is descriptive. It illustrates the reward geom-etry that the estimators present to the optimizer, not the resulting PPO trajectory or final policy. Second, the strict filter selects for cases of large LOO/LA divergence, and the cases here therefore over-represent the high-disagreement tail of the HIR-16K distribution. On samples where a single constraint dominates the teacher’s full-context con-ditioning, LA and LOO sum partially agree and the visual difference is subtler. Aggregate statistics across the full 15 ,450 -sample evaluation (Table 6, Figure 4) provide the corresponding non-cherry-picked summary. 

D Aggregation Method Ablation 

CC-OPD aggregates the per-constraint signals δi(t)

into the token-level shaping term δ(t) via summa-tion (Eq. (5)). Two natural alternatives are mean aggregation and top-k aggregation. We clarify the relation between mean and sum below, and report an empirical ablation of top-k=1 (max) and top-

k=2 against the default sum. 

Mean aggregation is a rescaled alternative. 

Replacing the sum in Eq. (5) by the mean 

δmean (t) = 1

> |C|

P 

> i

δi(t) divides the unclipped shaping signal for each sample by |C|. Under the Möbius decomposition in §B.3, this gives 

δmean = 1

> |C|

P  

> ∅̸ =T⊆C

|T | m(T ), so mean pre-serves the same relative interaction-order weighting as sum within a sample. It is therefore not ruled out by the decomposition. Because |C| varies across samples, however, mean also changes their relative shaping scale; a fair empirical comparison would require retuning λ and potentially the clip threshold 

c. We did not run that additional comparison, so the empirical ablation below is limited to sum and the two top-k variants. 

Top-k as a sparsified alternative. A natural sparsification of the sum is top-k selection: at each token, keep only the k largest-magnitude per-constraint shifts and discard the rest. The k=1 

case is the max aggregation, δ(t) = sign( δi∗ (t)) ·|δi∗ (t)| with i∗ = arg max i |δi(t)|; the k=2 case sums the two largest. Sparsified aggregation could in principle improve credit assignment by suppress-ing weak signal from constraints that are only marginally relevant to a given token, at the cost of discarding the multi-constraint interactions doc-umented in §B.4. 

Empirical comparison: Sum is the strongest aggregation tested. Table 8 reports MulDimIF accuracy on Overall and Level-4 for Sum, Top-

k=2 , and Top-k=1 under both model pairs, with all other hyperparameters fixed at paper-main val-ues ( λ=2 , c=5 , ρ=1 .0, 1,500 verl-steps). Sum is the strongest variant on the aggregate Overall met-ric on both model pairs (Qwen2.5: +1 .2 over Top-

k=2 , +1 .9 over Top-k=1 ; Qwen3: +2 .5 over Top-

k=2 , +1 .8 over Top-k=1 ). On the harder Level-4

split, aggregation differences narrow: Top-k=2 

marginally leads on Qwen2.5 ( +0 .3 over Sum), and Sum ties Top-k=1 on Qwen3 ( 63 .8). The spar-sified top-k aggregations underperform Sum on the Qwen2.5-1.5B Qwen3-4B Aggregation Overall Level-4 Overall Level-4 Sum 72.9 59.4 76.2 63.8                  

> Top-k=2 71.7 59.7 73.7 62.2 Top-k=1 71.0 57.9 74.4 63.8
> Table 8: Aggregation method ablation: MulDimIF accu-racy (%) under three aggregation methods for combin-ing the per-constraint signals δi(t)into the token-level shaping term δ(t). All variants share LOO counterfac-tual structure and identical hyperparameters ( λ= 2 ,
> c= 5 ,ρ= 1 .0,1,500 verl-steps); only the aggregation rule differs.

aggregate Overall metric, suggesting that discard-ing the cardinality-weighted contribution of multi-constraint interactions characterized in §B.3 out-weighs any credit-assignment gain. The summation aggregation we adopt is therefore the strongest of the three choices we tested on the aggregate metric, in addition to being the principled Möbius weight-ing; it additionally requires no per-token argmax computation and incorporates contributions from all |C| counterfactual teacher passes the framework already produces for each sample. 

E Sensitivity to Shaping Strength and Clip Threshold 

CC-OPD introduces two hyperparameters on top of the vanilla sampled-token OPD update: the shaping strength λ, which scales the counterfactual reward, and the clip threshold c, which bounds the aggre-gate per-token counterfactual shift δ(t) = P 

> i

δi(t).As defined in Eq. (6), clipping is applied after sum-mation: ˆδ(t) = clip( δ(t), −c, c ). Consequently, a large aggregate shift may arise either from one extreme per-constraint contrast or from the accu-mulation of several moderate contrasts. The bound 

|ˆδ(t)| ≤ c ensures that the absolute shaping con-tribution is at most λc per token. To characterize the joint behavior of λ and c around the paper-main configuration (λ=2 .0, c =5 .0) , we sweep a 

14 -cell cross-shaped grid on the Qwen2.5-1.5B / Qwen2.5-7B pair: a full c sweep at λ=2 .0 across 

c ∈ { 1, 2, 5, 10 , 20 }, a full λ sweep at c=5 .0 across 

λ ∈ { 0.5, 1, 2, 4}, and additional corner cells. All runs share the same 1,500 verl-step training hori-zon, training data, and PPO configuration as the main paper; only λ and c vary. Table 9 reports three validation benchmark scores (IFEval/IFBench/MulDimIF) per cell. The OPD baseline reference row and the paper-main cell (λ=2 .0, c =5 .0) follow the main-results evalu-ation protocol of Sec. 4.1. Within each benchmark column we mark the best score in bold and the second-best with an underline. 

A universal positive shift over OPD baseline. 

Every one of the 14 cells improves on the OPD baseline reference triple 70 .7 / 22 .8 / 69 .5 on all three benchmarks. Even the most extreme cor-ner (λ=0 .5, c =1) reaches 71 .5 / 24 .5 / 69 .7, with 

+0 .8 and +1 .7 pp gains on IFEval and IFBench while remaining at or above the OPD baseline on MulDimIF overall accuracy; the paper-main cell reaches 76 .0 / 28 .2 / 72 .9. The counterfactual sig-nal therefore provides positive return throughout the entire grid; the gain is not contingent on locat-ing a narrow operating point. 

Shaping strength traces a unimodal curve with empirical optimum at λ=2 . Holding the clip threshold fixed at c=5 , all three benchmarks trace a unimodal curve along λ. IFEval rises 73 .7 →

75 .4 → 76 .0 → 75 .7 as λ traverses {0.5, 1, 2, 4};IFBench follows a more pronounced unimodality 

25 .8 → 27 .6 → 28 .2 → 26 .2, with a clear 2.0 pp drop at λ=4 ; MulDimIF overall accuracy likewise peaks at λ=2 (71 .1 → 71 .4 → 72 .9 → 70 .9). Because λ scales the clipped shaping term ˆδ(t)

while leaving the vanilla OPD term unchanged, the decline at λ=4 is consistent with excessive shaping strength on this model pair. Among the tested values, the grid therefore identifies λ=2 as the empirical optimum. 

The default clip threshold performs best among the tested values. Holding λ=2 , the paper-main threshold c=5 attains the highest score on all three benchmarks among the five tested values. Across c ∈ { 1, 2, 5, 10 , 20 }, the score ranges are 

2.4 pp on IFEval, 2.6 pp on IFBench, and 2.3 pp on MulDimIF overall. For comparison, varying λ at 

c=5 produces ranges of 2.3, 2.4, and 2.0 pp on the same benchmarks. Performance therefore varies to a similar extent along both axes, and the effect of c

is non-monotonic over the tested values. We retain 

c=5 because it is the best tested threshold for this model pair. 

The paper-main cell is the joint optimum on a benign neighborhood. Aggregating the two axis findings, the paper-main configuration is the only cell that attains the best score on every bench-mark simultaneously. The per-benchmark runners-up are λ=2 , c =20 on IFEval ( 75 .8), λ=1 , c =5 c = 1 c = 2 c = 5 c = 10 c = 20                                                                                            

> λ= 0 .571 .5/24 .5/69 .772 .8/24 .7/70 .273 .7/25 .8/71 .1——
> λ= 1 .073 .0/24 .9/70 .673 .7/25 .3/70 .975 .4/27 .6/71 .4——
> λ= 2 .074 .3/25 .6/70 .673 .6/25 .8/71 .376 .0/28 .2/72 .9⋆75 .2/25 .9/71 .575 .8/27 .1/71 .2
> λ= 4 .0——75 .7/26 .2/70 .974 .7/27 .4/70 .273 .9/25 .5/69 .7
> OPD baseline 70 .7/22 .8/69 .5(reference; same evaluation protocol as the paper-main cell)
> Table 9: IFEval strict-prompt / IFBench strict-prompt / MulDimIF overall accuracy (%) for the (λ, c )ablation grid on Qwen2.5. Bold marks the best score on each benchmark; underline marks the second-best. The paper-main cell
> (λ=2 .0, c =5 .0) , marked ⋆, attains all three column-best scores.

on IFBench ( 27 .6), and λ=2 , c =10 on MulDimIF overall accuracy ( 71 .5). All three sit inside the 

λ ∈ { 1, 2}× c ∈ { 5, 10 , 20 } neighborhood that sur-rounds the paper-main cell. Failure modes appear only at the extreme corners (the tight-clip / weak-shaping corner that under-uses the counterfactual signal, and the high-λ row that over-uses it rela-tive to the baseline OPD reward), and even those corners retain positive net gain over OPD baseline on every benchmark. The paper-main configura-tion (λ=2 .0, c =5 .0) emerged from this sweep as the joint per-benchmark optimum, and we retain it throughout the rest of the paper. Practitioners de-ploying CC-OPD can expect comparable behavior at any operating point within the same neighbor-hood without re-tuning. 

F Constraint Subsampling: How Many Counterfactual Contexts Are Needed? 

The dominant per-sample cost of CC-OPD is the 

|C| + 1 teacher forward passes needed to score the full constraint set C and each leave-one-out variant. A natural way to amortize this cost is to subsam-ple counterfactual contexts: with subsampling ratio 

ρ ∈ (0 , 1] , we retain only ⌈ρ |C|⌉ leave-one-out contexts per sample and rescale the summed signal by |C|/⌈ρ |C|⌉ to preserve its expected magnitude. The main paper reports CC-OPD with ρ=1 .00 ;this appendix asks whether the dense leave-one-out scheme is necessary, or whether a sparser subsam-pled signal already captures the gain. 

Setup. We sweep ρ ∈ { 0.10 , 0.25 , 0.50 , 1.00 }

on the Qwen2.5-1.5B / Qwen2.5-7B pair, hold-ing all other hyperparameters fixed at λ=2 .0 and 

c=5 .0. The vanilla sampled-token OPD base-line is included as a reference. Per-step compute grows with ρ because the number of teacher for-ward passes per gradient step is approximately proportional to ρ |C|; the four CC-OPD configura-tions therefore have monotonically increasing per-step compute (qualitatively, ρ=1 .00 costs roughly 

3× more per step than ρ=0 .10 on the Qwen2.5-1.5B/7B pair). The ρ < 1.0 runs use 3,000 verl-steps, whereas the default ρ=1 .00 run uses 1,500 .Table 10 therefore compares selected checkpoints rather than equal numbers of updates; Figure 5 reports the complete gradient-step and FLOP tra-jectories. 

Comparison of subsampling-ratio variants across benchmarks. Table 10 reports the per-formance of four variants and OPD baseline on four instruction-following benchmarks. 

(1) All ρ > 0 substantially outperform OPD baseline on every benchmark. Even the spars-est configuration ρ=0 .10 , which uses less than a full set of leave-one-out contexts per sample on average, lifts the student by +1 .8 pp to +4 .4 pp across the four benchmarks. The counterfactual signal does not require dense sampling to provide supervision beyond what full-context-only OPD extracts; a partial estimate already breaks the OPD baseline ceiling. 

(2) The marginal value of higher ρ is benchmark-stratified. On MulDimIF Overall the ratio sweep saturates early: ρ=0 .25 already comes within 

0.4 pp of ρ=1 .00 (72 .5 vs. 72 .9), and ρ=0 .50 even regresses slightly ( 71 .6); the L1/L2/L3 sub-levels are dominated by the student’s general capabil-ity ceiling, dragging the aggregate flat. IFEval shows a similar saturation pattern, with ρ=0 .50 

tying ρ=1 .00 at 76 .0. On the constraint-stressed benchmarks the story is different. IFBench accu-racy improves monotonically from 25 .6 (ρ=0 .10 )to 28 .2 (ρ=1 .00 ), a +2 .6 pp gap from the sparsest to the densest configuration, on the same scale as the +2 .8 pp gain from OPD baseline to ρ=0 .10 on the same metric. MulDimIF Level-4 follows the same pattern, increasing from 57 .3 at ρ=0 .10 to 

59 .4 at ρ=1 .00 (with a minor non-monotone dip at Config IFEval IFBench MulDimIF Overall MulDimIF L4 

OPD baseline 70 .7 22 .8 69 .5 54 .9

CC-OPD ρ=0 .10 75 .1 (+4 .4) 25 .6 (+2 .8) 71 .3 (+1 .8) 57 .3 (+2 .4) 

CC-OPD ρ=0 .25 74 .5 (+3 .8) 26 .1 (+3 .3) 72 .5 (+3 .0) 58 .4 (+3 .5) 

CC-OPD ρ=0 .50 76 .0 (+5 .3) 26 .9 (+4 .1) 71 .6 (+2 .1) 58 .2 (+3 .3) 

CC-OPD ρ=1 .00 76 .0 (+5 .3) 28 .2 (+5 .4) 72 .9 (+3 .4) 59 .4 (+4 .5) 

Table 10: Cross-benchmark accuracy (%) for different Qwen2.5 subsampling-ratio CC-OPD variants and the OPD baseline. The IFEval, IFBench, and MulDimIF Overall entries in the OPD baseline and ρ=1 .00 rows reproduce Table 1; the ρ=1 .00 MulDimIF L4 entry reproduces Table 3 (3-sample mean under the unified evaluation protocol of §4.1). Parentheses are pp gains over the OPD baseline. Best ρ per column is bolded; ties are also bolded. 0 10000 20000                  

> Gradient Steps
> 0.50 0.55 0.60 0.65 0.70 0.75 0.0 0.5 1.0 1.5 2.0
> ×10 20 FLOPs
> 0.50 0.55 0.60 0.65 0.70 0.75 010000 20000
> Gradient Steps
> 0.30 0.35 0.40 0.45 0.50 0.55 0.60 0.0 0.5 1.0 1.5 2.0
> ×10 20 FLOPs
> 0.30 0.35 0.40 0.45 0.50 0.55 0.60
> MulDimIF-Overall MulDimIF-Level-4
> OPD r=0.1 r=0.25 r=0.5 r=1.0

Figure 5: Constraint subsampling ratio sweep on Qwen2.5-1.5B. MulDimIF Overall (columns 1–2) and Level-4 (columns 3–4) accuracy for OPD baseline (gray dashed) and four CC-OPD configurations with ρ ∈{0.10 , 0.25 , 0.50 , 1.00 }. Within each metric, the left column plots accuracy against gradient steps, the right column against cumulative training FLOPs. Curves are EMA-smoothed (weight 0.85 ) with the raw trajectory shown as a faint shadow. Higher ρ has higher per-step compute, so the same number of gradient steps consumes more FLOPs; conversely, at the same FLOPs budget, lower ρ has executed more steps. 

ρ=0 .50 ); the resulting +2 .1 pp gap nearly matches the +2 .4 pp OPD baseline-to-ρ=0 .10 jump. 

(3) The benchmark stratification recapitulates the main-paper constraint-dilution story. The metrics where ρ keeps mattering (IFBench and MulDimIF Level-4) are precisely the constraint-stressed regimes where the main paper argues full-context teacher distributions most dilute per-constraint information. The metrics where ρ

saturates early (MulDimIF Overall, dominated by easier sub-levels) are regimes where gen-eral instruction-following capability rather than constraint-level discrimination is the bottleneck. Denser counterfactual coverage helps exactly where per-constraint attribution matters most, which is the same physical effect that motivates the method itself, viewed through a different axis. Where the main paper varies constraint count |C|

(Level-1 to Level-4), this ablation varies the frac-tion of constraints whose contribution is explicitly attributed. 

The compute–quality picture. Figure 5 plots the five MulDimIF trajectories on both a gradient-step axis (columns 1 and 3) and an absolute-FLOPs axis (columns 2 and 4). The two views corre-spond to two different deployment questions. Un-der iso-gradient-step (the left view of each met-ric), higher ρ trains faster per step. Under iso-FLOPs (the right view of each metric), higher ρ

costs more per step, so a lower ρ trained for more steps occupies a different region of the curve. Read-ing the columns together, no CC-OPD configura-tion is Pareto-dominated by OPD baseline on any axis; among CC-OPD configurations, the choice of ρ trades total compute against quality on the constraint-stressed slices. 

Why the main paper uses ρ=1 .00 . The con-tribution claim of CC-OPD, namely that per-constraint counterfactual attribution surfaces super-vision a single full-context teacher pass conflates, is most directly demonstrated in the constraint-stressed regimes. These are exactly the metrics on which ρ=1 .00 delivers gains of +2 .6 pp on IF-Bench and +2 .1 pp on MulDimIF Level-4 over the ρ=0 .10 configuration. On IFBench in par-ticular, the “densification” gap from ρ=0 .10 to 

ρ=1 .00 is on the same scale as the “existence” gap from no counterfactual signal (OPD baseline) to a sparse one ( ρ=0 .10 ). Reporting CC-OPD at 

ρ=1 .00 therefore characterizes the upper end of the leave-one-out estimator’s quality; sparser counter-factual sampling is a viable compute-saving option for applications that do not bottleneck on constraint-level discrimination, where ρ ∈ [0 .25 , 0.50] re-covers most of the gain on aggregate metrics like MulDimIF Overall and IFEval at a fraction of the per-step cost.