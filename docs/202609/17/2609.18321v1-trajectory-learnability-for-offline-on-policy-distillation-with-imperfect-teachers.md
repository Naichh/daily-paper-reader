---
title: Trajectory Learnability for Offline On-Policy Distillation with Imperfect Teachers
title_zh: 面向不完美教师的离线同策略蒸馏中的轨迹可学习性
authors: "Yihao Ai, Weilong Yan"
date: 2026-09-16
pdf: "https://arxiv.org/pdf/2609.18321v1"
tags: ["query:policy-dist"]
score: 10.0
evidence: 面向不完美教师的离线在线策略蒸馏，研究教师监督的可学习性
tldr: 离线在线策略蒸馏通过复用学生轨迹与教师监督提升效率，但也使不完美监督持续影响优化。本文提出轨迹可学习性：先在教师成功问题上训练，再测量教师失败问题轨迹中各词元似然变化，将正向变化聚合成轨迹权重用于蒸馏损失。该方法无需额外生成，可用于数学推理与代码生成，较离线基线最高提升2.7个百分点，并媲美或超越在线OPD。其仅需2块GPU约22小时，显著低于在线方法的3块GPU和36–48小时。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2609.18321v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1324, \"height\": 440, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.18321v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1404, \"height\": 455, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2609.18321v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1393, \"height\": 769, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2609.18321v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1408, \"height\": 724, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.18321v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 909, \"height\": 240, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2609.18321v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 894, \"height\": 292, \"label\": \"Table\"}]"
motivation: 离线OPD复用轨迹和监督，导致不完美教师错误持续存在；教师失败仅是问题级粗信号，并非整条轨迹都不可学。
method: 用教师成功问题训练作参考，计算失败问题轨迹词元似然变化，作为可学习性信号，聚合成轨迹权重加权原蒸馏损失。
result: 在数学推理与代码生成上，较离线OPD基线最高提升2.7个百分点，并在多个基准匹配或超越在线OPD。
conclusion: 方法无需额外生成，可一次计算完成，且仅用2块GPU约22小时，比代表性在线OPD更高效。
---

## 摘要
离线同策略蒸馏通过一次性收集学生轨迹与教师监督信号，并在整个优化过程中重复使用，从而获得效率优势。但同样的重复使用也使得不完美的监督持续存在。由于即便强大的教师也会出错，我们提出：在不完美的教师监督下，究竟还有什么仍然可学？教师失败只是一个粗粒度的问题级信号，并不意味着与之对应的学生轨迹上的所有监督都无益。一个自然的替代方案是估计教师沿该轨迹的可恢复性，但重复续写会在很大程度上抵消离线蒸馏的效率优势。我们转而使用教师成功的问题来定义一个廉价的参考，以刻画学生能够学到什么。我们在教师成功的问题上训练，并测量来自教师失败问题的轨迹中每个被观测 token 的似然如何变化。我们将这些带符号的似然变化作为一种可操作的可学习性信号：增幅越大，表明该行为越受到仅用成功样本学习的促进。我们将该信号聚合为轨迹级权重，用于原始蒸馏损失。与基于续写的估计不同，我们的可学习性不需要额外生成，且可由存储的轨迹与模型检查点一次性计算得到。在数学推理与代码生成任务上，我们的方法将离线 OPD 基线提升最多 2.7 个百分点，并在多个基准上达到或超过在线 OPD 变体。尽管增加了仅用成功样本的蒸馏阶段，它仅需 2 块 GPU 和约 22 GPU 小时，而代表性的在线 OPD 方法需要 3 块 GPU 和 36–48 GPU 小时。

## Abstract
Offline on-policy distillation gains efficiency by collecting student trajectories and teacher supervision once and reusing them throughout optimization. The same reuse makes imperfect supervision persistent. Since even strong teachers can fail, we ask \emph{what remains learnable from imperfect teacher supervision?} Teacher failure is only a coarse problem-level signal and does not imply that all supervision along the associated student trajectory is unhelpful. A natural alternative is to estimate teacher recoverability along the trajectory, but repeated continuations largely erase the efficiency advantage of offline distillation. We instead use teacher-successful problems to define a cheap reference for what the student can learn. We train on teacher-successful problems and measure how the likelihood of each observed token in trajectories from teacher-failed problems changes. We use these signed likelihood changes as an operational \emph{learnability signal}: larger increases indicate behavior more strongly promoted by successful-only learning. We aggregate this signal into trajectory-level weights for the original distillation loss. Unlike continuation-based estimates, our learnability requires no additional generation and can be computed once from stored trajectories and model checkpoints. Across mathematical reasoning and code generation, our method improves an offline OPD baseline by up to 2.7 percentage points and matches or outperforms online OPD variants on multiple benchmarks. Despite the additional successful-only distillation stage, it uses 2 GPUs and about 22 GPU hours, compared with 3 GPUs and 36--48 GPU hours for representative online OPD methods.

---

## 论文详细总结（自动生成）

# 论文总结：Trajectory Learnability for Offline On-Policy Distillation with Imperfect Teachers

## 1. 核心问题与整体含义
- **研究背景**：离线同策略蒸馏（Offline OPD）通过一次性收集学生轨迹和教师 token 级监督，并在整个优化过程中重复使用，显著降低训练成本。
- **核心矛盾**：同一批监督会被反复使用，因此教师的不完美监督会持续影响学生优化。即使强教师也会在部分问题上失败。
- **关键问题**：教师失败只是一个**问题级粗标签**，并不代表该学生轨迹上所有条件监督都无用；但直接估计“教师沿轨迹的可恢复性”需要大量前缀续写与验证，会抵消离线蒸馏的效率优势。
- **论文主张**：不应简单保留或丢弃教师失败样本，而应估计每条教师失败轨迹中“还剩多少可学习性”，并据此分配蒸馏监督强度。

## 2. 方法论：Learnability-Weighted Distillation, LWD
### 核心思想
- 使用**教师成功问题**上的学习作为参考更新，观察该更新如何改变教师失败问题上已存储学生 token 的似然。
- 将这种**带符号的似然变化**作为 token 级可学习性信号，再聚合成轨迹级权重，用于缩放原始 OPD 损失。
- 不需要额外生成，只需对存储轨迹做两次前向打分。

### 算法流程
1. **构造离线记录**
   - 固定学生策略 \(\rho\) 生成轨迹 \(\tau_i=(a_{i,1},...,a_{i,T_i})\)。
   - 冻结教师 \(\pi_T\) 在学生访问状态 \(s_{i,t}=(x_i,a_{i,<t})\) 上提供 token 级监督。
   - 教师独立生成回答 \(y_i^T\)，用成功谓词得到标签 \(c_i\)，划分：
     - \(D^+=\{i:c_i=1\}\)：教师成功问题；
     - \(D^-=\{i:c_i=0\}\)：教师失败问题。
   - 注意：\(c_i\) 是问题级教师结果，不判断学生轨迹或每个教师目标的质量。

2. **成功-only 参考学习**
   - 从同一初始策略 \(\rho\) 出发，仅在 \(D^+\) 上做蒸馏训练，得到参考模型 \(\pi^+\)。
   - 论文中该阶段训练 150 个优化步。
   - 该更新称为 successful-only learning，表示“从教师成功监督中带来的模型变化”。

3. **Token 级可学习性信号**
   - 对每个 \(i\in D^-\)，用同一批存储 token 分别在 \(\rho\) 和 \(\pi^+\) 下打分：
     \[
     \ell_{i,t}=\log \pi^+(a_{i,t}|s_{i,t})-\log \rho(a_{i,t}|s_{i,t})
     \]
   - 正值表示成功-only 学习提高了该 token 的似然，负值表示降低。
   - 轨迹平均信号满足：
     \[
     \bar\ell_i=\frac{1}{T_i}\log\frac{\pi^+(\tau_i|x_i)}{\rho(\tau_i|x_i)}
     \]
   - 一阶解释：平均信号近似反映成功-only 更新方向与提高该轨迹似然方向的对齐程度。
   - 实现中对 \(\ell_{i,t}\) 裁剪到 \([-4,4]\)，并用 33-token 滑动平均平滑。

4. **轨迹级聚合与加权**
   - 对平滑信号计算每条轨迹的均值 \(m_i\) 和标准差 \(v_i\)，并在 \(D^-\) 内标准化为 \(z_m,z_v\)。
   - 分三档：
     - low：\(z_m\le -0.5\) 或 \(z_v\ge 1.0\)；
     - high：\(z_m\ge 0.5\) 且 \(z_v<1.0\)；
     - mid：其余。
   - 对应名义权重：low=0.15，mid=0.25，high=0.35；再做全局缩放，使 \(D^-\) 的响应 token 加权平均权重约为 0.25，并裁剪到 \([0.15,0.
